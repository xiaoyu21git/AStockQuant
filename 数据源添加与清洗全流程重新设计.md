# 数据源添加与清洗全流程重新设计

## 一、设计原则与目标

### 1.1 核心设计原则
1. **优先使用现有工具类**：充分利用Foundation、Engine、Infrastructure库
2. **减少Qt依赖**：在可能的情况下使用标准C++或现有库替代Qt类
3. **单一职责**：每个类只负责一个明确的功能
4. **异步优先**：耗时操作使用异步，保持UI响应性
5. **错误可恢复**：完善的错误处理和恢复机制
6. **缓存智能**：智能缓存策略，避免重复查询

### 1.2 设计目标
1. **完整的端到端流程**：从数据源选择到数据准备的全链路
2. **清晰的状态管理**：每个步骤都有明确的状态和进度反馈
3. **可扩展的架构**：支持新的数据源类型和清洗规则
4. **高性能处理**：支持大数据量的高效处理
5. **用户友好交互**：提供实时反馈和进度显示

## 二、当前实现问题全面检查

### 2.1 数据查询功能问题
| 问题类型 | 具体表现 | 文件 | 影响 |
|---------|---------|------|------|
| 接口不一致 | 多个queryDailyDataAsync方法签名不同 | DatabaseQueryService.cpp<br>DataService_Simple.cpp | 调用混乱 |
| 类型混乱 | DataSourceType_Simple与DataSourceType并存 | DataService_Simple.h/.cpp | 编译错误 |
| 简化实现 | 使用QTimer模拟查询 | DataFetchController_v2.cpp | 功能不完整 |
| 错误处理不完善 | 异常处理缺乏恢复机制 | 多个文件 | 系统不稳定 |

### 2.2 数据清洗功能问题
| 问题类型 | 具体表现 | 文件 | 影响 |
|---------|---------|------|------|
| 编译错误 | generateCacheKey函数签名不匹配 | DataCleaningService_fixed.cpp | 无法编译 |
| 规则转换不完整 | convertRules方法简化实现 | DataCleaningService_fixed.cpp | 功能受限 |
| 缓存机制不完善 | 缓存键生成逻辑可能冲突 | DataCleaningService_fixed.cpp | 缓存效率低 |
| 线程管理混乱 | QThreadPool与自定义线程混用 | 多个文件 | 性能问题 |

### 2.3 缓存功能问题
| 问题类型 | 具体表现 | 文件 | 影响 |
|---------|---------|------|------|
| 实现不完整 | DataCacheService.cpp缺少完整实现 | DataCacheService.cpp | 链接错误 |
| 缓存策略简单 | 缺乏智能过期和清理机制 | DataCacheService.h | 内存占用高 |
| 键设计混乱 | 缓存键生成逻辑不一致 | 多个文件 | 缓存命中率低 |

### 2.4 架构设计问题
| 问题类型 | 具体表现 | 影响 |
|---------|---------|------|
| 服务职责不清 | 多个数据服务并存，边界模糊 | 代码重复，维护困难 |
| 流程控制分散 | 数据流程分散在多个控制器中 | 难以跟踪完整流程 |
| 状态管理混乱 | 缺少统一的状态管理机制 | 错误恢复困难 |

## 三、重新设计的完整流程

### 3.1 整体流程架构
```
┌─────────────────────────────────────────────────────────────┐
│                      UI层 (QML)                             │
│  DataSourceModal.qml → DataCleaningModal.qml → 下一步界面   │
└───────────────────┬─────────────────────────────────────────┘
                    │ (信号/槽)
                    ▼
┌─────────────────────────────────────────────────────────────┐
│                控制器层 (DataFlowController)                 │
│  管理完整的数据流程，协调各个服务，提供统一的API接口         │
└───────────────────┬─────────────────────────────────────────┘
                    │ (服务调用)
                    ▼
┌─────────────────────────────────────────────────────────────┐
│                 服务协调层 (DataFlowOrchestrator)            │
│  编排数据查询、缓存、清洗的完整流程，处理错误和重试           │
└──────┬────────────┬──────────────────────────────┬──────────┘
       │            │                              │
       ▼            ▼                              ▼
┌────────────┐┌────────────┐              ┌─────────────┐
│  查询服务  ││  缓存服务  │              │  清洗服务   │
│DataQuery-  ││DataCache-  │              │DataCleaning-│
│ Service    ││ Service    │              │ Service     │
└─────┬──────┘└─────┬──────┘              └─────┬───────┘
      │             │                           │
      ▼             ▼                           ▼
┌────────────┐┌────────────┐              ┌─────────────┐
│ 现有工具类 ││ 现有工具类 │              │ 现有工具类  │
│QueryBuilder││DataService-│              │DataCleaning-│
│Connection- ││ Cache      │              │ Engine      │
│ Pool       ││            │              │             │
└────────────┘└────────────┘              └─────────────┘
```

### 3.2 详细流程步骤

#### 步骤1：数据源配置（DataSourceModal.qml）
**用户操作**：
1. 选择数据提供商（聚宽、掘金、数据库等）
2. 选择交易所/市场
3. 输入或选择股票代码（支持多选）
4. 设置时间范围（开始日期和结束日期）
5. 选择数据类型（日线、周线、分钟线等）

**后端处理**：
```cpp
// DataFlowController::configureDataSource()
1. 验证参数有效性
2. 生成唯一请求ID
3. 创建数据源配置对象
4. 发布"DataSourceConfigured"事件
```

#### 步骤2：数据查询与缓存
**自动触发**：用户点击"加载数据"或配置完成后自动开始
**处理流程**：
```cpp
// DataFlowOrchestrator::executeDataQuery()
1. 检查缓存（使用DataCacheService）
   - 如果缓存命中：直接使用缓存数据，跳转到步骤4
   - 如果缓存未命中：继续执行查询
   
2. 执行数据库查询（使用DataQueryService）
   - 连接数据库（使用现有ConnectionPool）
   - 构建查询（使用现有QueryBuilder）
   - 执行异步查询
   - 处理查询结果，转换为标准格式
   
3. 缓存查询结果（使用DataCacheService）
   - 生成缓存键（基于请求参数哈希）
   - 存储数据到缓存
   - 更新缓存统计
   
4. 发布"DataQueryCompleted"事件
```

#### 步骤3：清洗规则配置（DataCleaningModal.qml）
**用户操作**：
1. 查看原始数据统计信息
2. 选择预设清洗规则集（技术分析、基本面分析等）
3. 自定义清洗规则
   - 价格范围过滤
   - 成交量过滤
   - 时间范围过滤
   - 异常值处理
   - 数据完整性检查
4. 预览清洗效果

**后端处理**：
```cpp
// DataFlowController::configureCleaningRules()
1. 获取原始数据统计
2. 加载预设规则集
3. 应用用户自定义规则
4. 执行同步预览清洗
5. 返回预览结果和统计
```

#### 步骤4：数据清洗执行
**用户操作**：点击"执行数据清洗"
**处理流程**：
```cpp
// DataFlowOrchestrator::executeDataCleaning()
1. 检查清洗缓存（如果启用）
   - 如果相同数据和规则的清洗结果已缓存：直接使用缓存结果
   
2. 执行异步清洗（使用DataCleaningService）
   - 转换规则格式（QVariantMap → 清洗引擎格式）
   - 执行异步清洗任务
   - 实时报告清洗进度
   
3. 缓存清洗结果（如果启用）
   - 生成清洗结果缓存键
   - 存储清洗后的数据
   
4. 发布"DataCleaningCompleted"事件
   - 包含清洗统计信息
   - 包含清洗后的数据
```

#### 步骤5：数据预览与准备
**自动触发**：清洗完成后自动进入
**处理流程**：
```cpp
// DataFlowController::prepareDataForNextStep()
1. 生成数据质量报告
   - 原始数据统计
   - 清洗过程统计
   - 数据质量评分
   
2. 准备数据导出格式
   - 标准化数据结构
   - 添加元数据信息
   
3. 发布"DataReadyForNextStep"事件
   - 包含处理后的数据
   - 包含数据质量报告
   - 包含处理过程摘要
```

#### 步骤6：传递到下一步骤
**用户操作**：点击"下一步"或自动传递
**处理流程**：
```cpp
// 下一步处理模块
1. 监听"DataReadyForNextStep"事件
2. 获取处理后的数据
3. 开始后续分析或处理
```

### 3.3 状态管理与错误处理

#### 状态管理
```cpp
enum class DataFlowState {
    IDLE,                    // 空闲
    CONFIGURING_SOURCE,      // 配置数据源
    QUERYING_DATA,           // 查询数据中
    CACHING_DATA,            // 缓存数据中
    CONFIGURING_CLEANING,    // 配置清洗规则
    PREVIEWING_CLEANING,     // 预览清洗效果
    EXECUTING_CLEANING,      // 执行清洗中
    PREPARING_DATA,          // 准备数据中
    READY_FOR_NEXT,          // 准备就绪
    ERROR,                   // 错误状态
    CANCELLED                // 已取消
};
```

#### 错误处理策略
1. **可恢复错误**：自动重试（最多3次）
2. **部分失败**：继续处理可处理的部分，记录失败部分
3. **致命错误**：停止处理，回滚到安全状态，通知用户
4. **超时处理**：设置操作超时，超时后自动取消或重试

## 四、新的核心组件设计

### 4.1 DataFlowController（新组件）
**职责**：提供统一的API接口，管理UI交互
**核心功能**：
- 接收UI请求，转换为内部命令
- 管理数据流程状态
- 提供进度反馈和错误通知
- 协调数据传递到下一步骤

### 4.2 DataFlowOrchestrator（新组件）
**职责**：编排完整的数据处理流程
**核心功能**：
- 调用各个服务执行具体操作
- 管理流程依赖和顺序
- 处理错误和重试逻辑
- 生成处理日志和统计

### 4.3 优化的DataQueryService
**基于现有工具类重构**：
```cpp
class DataQueryService {
private:
    // 使用现有工具类
    std::shared_ptr<astock::database::ConnectionPool> m_connectionPool;
    std::unique_ptr<astock::database::QueryBuilder> m_queryBuilder;
    
public:
    // 统一查询接口
    QueryResult queryData(const DataQueryRequest& request) {
        // 使用现有QueryBuilder构建查询
        auto builder = createQueryBuilder(request.dataType);
        // 使用现有ConnectionPool获取连接
        auto connection = m_connectionPool->acquireConnection();
        // 执行查询
        return executeQuery(builder, connection);
    }
};
```

### 4.4 优化的DataCleaningService
**基于现有工具类重构**：
```cpp
class DataCleaningService {
private:
    // 使用现有清洗引擎
    std::unique_ptr<DataCleaningEngine> m_cleaningEngine;
    // 使用Foundation线程池
    std::shared_ptr<foundation::thread::IExecutor> m_threadPool;
    
public:
    // 统一清洗接口
    CleaningResult cleanData(const CleaningRequest& request) {
        // 转换规则格式
        auto rules = convertRules(request.rules);
        // 使用现有清洗引擎
        return m_cleaningEngine->cleanData(request.data, rules);
    }
};
```

### 4.5 优化的DataCacheService
**基于现有工具类重构**：
```cpp
class DataCacheService {
private:
    // 使用现有缓存系统
    DataServiceCache& m_cache = DataServiceCache::getInstance();
    
public:
    // 智能缓存策略
    CacheResult getOrQuery(const CacheKey& key, std::function<QueryResult()> queryFunc) {
        // 检查缓存
        if (auto cached = m_cache.get(key)) {
            return CacheResult::hit(cached);
        }
        
        // 缓存未命中，执行查询
        auto result = queryFunc();
        
        // 缓存结果
        if (shouldCache(result)) {
            m_cache.put(key, result);
        }
        
        return CacheResult::miss(result);
    }
};
```

## 五、接口设计与数据格式

### 5.1 统一请求格式
```cpp
struct DataFlowRequest {
    QString requestId;          // 唯一请求ID
    DataSourceConfig source;    // 数据源配置
    CleaningRulesConfig rules;  // 清洗规则配置
    ProcessingOptions options;  // 处理选项
    QDateTime timestamp;        // 请求时间戳
};
```

### 5.2 统一响应格式
```cpp
struct DataFlowResponse {
    QString requestId;          // 对应请求ID
    DataFlowState state;        // 处理状态
    ProcessingResult result;    // 处理结果
    QualityReport quality;      // 质量报告
    ErrorInfo error;           // 错误信息（如果有）
    QDateTime completionTime;   // 完成时间
};
```

### 5.3 事件定义
使用现有EventBus系统发布事件：
```cpp
// 关键事件
EVENT(DataSourceConfigured, sourceConfig)
EVENT(DataQueryStarted, requestId)
EVENT(DataQueryProgress, requestId, progress, message)
EVENT(DataQueryCompleted, requestId, result, cached)
EVENT(CleaningRulesConfigured, requestId, rules)
EVENT(DataCleaningStarted, requestId)
EVENT(DataCleaningProgress, requestId, progress, message)
EVENT(DataCleaningCompleted, requestId, result, stats)
EVENT(DataReadyForNextStep, requestId, preparedData)
EVENT(DataFlowError, requestId, error)
```

## 六、实施计划

### 6.1 第一阶段：基础组件重构（1周）
1. **修复编译问题**（2天）
   - 统一类型定义（DataSourceType等）
   - 修复函数签名不匹配
   - 确保项目可编译
   
2. **创建核心协调组件**（3天）
   - DataFlowController
   - DataFlowOrchestrator
   - 统一的事件定义

### 6.2 第二阶段：服务优化（1周）
1. **优化DataQueryService**（2天）
   - 统一查询接口
   - 使用现有工具类
   - 完善错误处理
   
2. **优化DataCleaningService**（2天）
   - 统一清洗接口
   - 完善规则转换
   - 优化缓存策略
   
3. **优化DataCacheService**（1天）
   - 智能缓存策略
   - 统一的缓存接口

### 6.3 第三阶段：UI集成与测试（1周）
1. **更新UI界面**（2天）
   - 更新DataSourceModal.qml使用新接口
   - 更新DataCleaningModal.qml使用新接口
   - 添加进度显示和状态反馈
   
2. **端到端测试**（3天）
   - 单元测试各个服务
   - 集成测试完整流程
   - 性能测试和优化

### 6.4 第四阶段：优化与部署（3天）
1. **性能优化**（1天）
2. **错误处理完善**（1天）
3. **文档和示例**（1天）

## 七、成功标准

### 7.1 功能标准
1. ✅ 完整的数据源添加流程可工作
2. ✅ 数据查询、缓存、清洗功能完整
3. ✅ 支持实时进度反馈和状态显示
4. ✅ 支持错误恢复和重试
5. ✅ 数据可正确传递到下一步骤

### 7.2 性能标准
1. ✅ 数据查询响应时间 < 3秒（本地数据库）
2. ✅ 清洗处理速度 > 1000条/秒
3. ✅ 内存使用稳定，无内存泄漏
4. ✅ UI响应时间 < 100毫秒

### 7.3 质量标准
1. ✅ 代码编译零错误
2. ✅ 关键功能有自动化测试覆盖
3. ✅ 错误处理完善，不会崩溃
4. ✅ 代码符合设计原则和规范

## 八、风险评估与应对

### 8.1 技术风险
**风险**：现有工具类不完整或不兼容
**应对**：先验证工具类可用性，准备备选方案

### 8.2 集成风险
**风险**：新组件与现有系统集成困难
**应对**：分阶段集成，保持向后兼容

### 8.3 性能风险
**风险**：新架构性能不如预期
**应对**：早期性能测试，持续优化

## 九、下一步行动建议

### 9.1 立即行动
1. **验证现有工具类**：确认Foundation、Engine、Infrastructure库的可用性
2. **创建技术验证原型**：验证核心流程的可行性
3. **制定详细开发计划**：基于本文档细化开发任务

### 9.2 开发建议
1. **采用测试驱动开发**：先写测试，再写实现
2. **分模块并行开发**：不同开发者负责不同组件
3. **持续集成验证**：每次提交都验证编译和基本功能

### 9.3 文档建议
1. **更新API文档**：为新组件创建详细API文档
2. **创建用户指南**：为最终用户创建操作指南
3. **维护技术决策记录**：记录重要的技术决策和原因

---

**文档版本**：1.0  
**制定日期**：2026年2月24日  
**制定依据**：  
- 《项目状态总结与修改方向.md》  
- 《数据清洗与缓存功能设计方案.md》  
- 《DataService_基于现有工具类的重构方案.md》  
- 项目实际代码分析