# 数据源和清洗功能重构计划

## 目标
对数据源添加、规则设置和清洗预览功能进行全面逻辑重构，确保功能完全正常使用，同时优化数据库和缓存系统的集成。

## 当前状态分析

### 1. 数据源添加功能 (DataSourceModal.qml)
**状态**：UI实现相对完整，使用DataFetchController与DataService交互
**问题**：
- DataFetchController可能需要完善与DataService的连接
- 数据源添加后的完整流程需要验证
- 数据预览功能需要测试

### 2. 规则设置和清洗预览 (DataCleaningEngine)
**状态**：DataCleaningEngine类功能完善，支持多种清洗规则
**问题**：
- 需要验证与DataService的集成
- 清洗预览功能需要测试
- 规则转换逻辑需要验证

### 3. 数据库系统 (DataService + QtMySQLDatabase)
**状态**：DataService依赖QtMySQLDatabase进行数据操作
**问题**：
- 数据库初始化流程需要验证
- 查询构建器使用方式需要统一
- 数据库连接池管理需要优化

### 4. 缓存系统 (DataServiceCache)
**状态**：缓存系统已设计，需要验证与DataService的集成
**问题**：
- 缓存命中率需要监控
- 缓存失效策略需要测试
- 与清洗引擎的缓存集成需要验证

### 5. 编译问题
**已知问题**：
1. EventFormat.hpp中的`set<int&>`模板实例化错误
2. DataService.cpp中的私有成员重复定义
3. GlobalEventBusRegistry的使用方式问题

## 重构实施计划

### 第一阶段：修复编译问题（1天）
**目标**：确保项目能正常编译

#### 1.1 修复EventFormat.hpp问题
```cpp
// 问题：event.set("data_count", dataCount)中的dataCount是int&
// 解决方案：确保EventValue能正确处理int引用
```

#### 1.2 修复DataService.cpp中的重复定义
```cpp
// DataService.cpp第303-306行有重复的私有成员定义
// 需要检查DataLoadTask、DataCleaningTask、DataSaveTask的定义
```

#### 1.3 修复EventBus初始化问题
```cpp
// DataService.cpp第334行：GlobalEventBusRegistry的使用方式
```

### 第二阶段：数据源添加功能重构（2天）
**目标**：确保数据源能正常添加，数据能正确加载

#### 2.1 完善DataFetchController
- 验证与DataService的信号槽连接
- 完善数据源添加逻辑
- 添加错误处理和重试机制

#### 2.2 完善DataSourceModal.qml
- 验证日期选择器功能
- 完善数据预览功能
- 添加加载状态显示

#### 2.3 测试数据源添加流程
- 测试数据源添加 -> 数据加载 -> 数据预览全流程
- 验证数据保存到数据库
- 验证数据从数据库加载

### 第三阶段：规则设置和清洗功能重构（2天）
**目标**：确保清洗规则能正常设置，清洗预览功能正常

#### 3.1 完善DataCleaningEngine集成
- 验证DataCleaningEngine与DataService的集成
- 测试清洗规则设置和保存
- 验证规则转换逻辑

#### 3.2 完善清洗预览功能
- 实现实时清洗预览
- 添加清洗统计信息显示
- 验证清洗结果正确性

#### 3.3 测试清洗流程
- 测试数据加载 -> 规则设置 -> 清洗预览全流程
- 验证清洗结果保存
- 测试批量清洗功能

### 第四阶段：数据库和缓存系统优化（2天）
**目标**：确保数据库和缓存系统稳定高效

#### 4.1 数据库连接优化
- 验证数据库连接池管理
- 优化查询构建器使用
- 添加数据库错误恢复机制

#### 4.2 缓存系统集成
- 验证DataServiceCache与DataService的集成
- 测试缓存命中率
- 优化缓存失效策略

#### 4.3 性能监控
- 添加数据库查询性能监控
- 添加缓存命中率监控
- 添加内存使用监控

### 第五阶段：集成测试和优化（1天）
**目标**：确保所有功能协同工作

#### 5.1 端到端测试
- 测试数据源添加 -> 数据加载 -> 规则设置 -> 清洗预览全流程
- 验证数据持久化
- 验证缓存效果

#### 5.2 性能优化
- 优化大数据集处理性能
- 优化内存使用
- 优化线程池使用

#### 5.3 错误处理
- 完善错误处理机制
- 添加用户友好的错误提示
- 添加自动重试机制

## 技术重点

### 1. 信号槽机制优化
- 确保DataFetchController、DataService、DataCleaningEngine之间的信号槽连接正确
- 避免信号丢失或延迟
- 添加超时处理机制

### 2. 数据流管理
- 统一数据格式：QVariantList -> QVariantMap
- 统一错误处理机制
- 统一进度报告机制

### 3. 资源管理
- 数据库连接池管理
- 线程池管理
- 缓存内存管理

### 4. 用户界面反馈
- 实时进度显示
- 错误信息提示
- 操作状态反馈

## 风险评估

### 高风险
1. **EventFormat模板问题**：可能导致编译失败
2. **数据库连接问题**：可能导致数据加载失败
3. **信号槽连接问题**：可能导致功能不响应

### 中风险
1. **缓存一致性问题**：可能导致数据显示不一致
2. **线程安全问题**：多线程操作可能导致数据竞争
3. **内存泄漏**：长时间运行可能导致内存增长

### 低风险
1. **UI交互问题**：用户界面细节问题
2. **性能问题**：大数据集处理性能

## 成功标准

1. **功能正常**：数据源添加、规则设置、清洗预览功能完全正常
2. **性能达标**：大数据集处理响应时间可接受
3. **稳定性高**：长时间运行无内存泄漏或崩溃
4. **用户体验好**：操作流畅，反馈及时
5. **代码质量高**：代码结构清晰，易于维护

## 立即行动

### 今天可以开始：
1. 修复编译错误
2. 验证DataFetchController与DataService的连接
3. 测试基本的数据加载功能

### 明天计划：
1. 完善数据源添加流程
2. 开始清洗功能集成
3. 初步测试数据库和缓存功能

这个计划遵循渐进式重构原则，每个阶段都有明确的产出和验证标准，确保重构过程可控，风险可管理。