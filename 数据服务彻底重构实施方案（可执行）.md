# 数据服务彻底重构实施方案（可执行）

## 项目概述
**目标**：彻底重构数据源添加与清洗功能，解决65个链接错误，建立健壮、简洁、可维护的新架构  
**核心原则**：健壮性、简化、实用、不过度设计  
**时间估算**：5-7个工作日  
**最终交付物**：不超过300行代码的DataService，功能完整，程序不崩溃

## 一、重构前准备（第1天）

### 任务1.1：代码备份与验证
**目标**：确保有完整的备份，验证现有工具类可用性  
**预计时间**：2-3小时

#### 具体步骤：
```bash
# 1. 创建备份目录
mkdir -p "g:/C++/AStockQuantEngine/backup/数据功能旧代码"

# 2. 备份所有将被删除的文件
cp "src/ui/bridge/include/DataFetchController_v2.h" "backup/数据功能旧代码/"
cp "src/ui/bridge/src/DataFetchController_v2.cpp" "backup/数据功能旧代码/"
cp "src/ui/bridge/include/DataService_Simple.h" "backup/数据功能旧代码/"
cp "src/ui/bridge/src/DataService_Simple.cpp" "backup/数据功能旧代码/"
cp "src/ui/bridge/include/DatabaseQueryService.h" "backup/数据功能旧代码/"
cp "src/ui/bridge/src/DatabaseQueryService.cpp" "backup/数据功能旧代码/"
cp "src/ui/bridge/include/DataCacheService.h" "backup/数据功能旧代码/"
cp "src/ui/bridge/src/DataCacheService.cpp" "backup/数据功能旧代码/"
cp "src/ui/bridge/include/DataSourceManager.h" "backup/数据功能旧代码/"
cp "src/ui/bridge/src/DataSourceManager.cpp" "backup/数据功能旧代码/"
cp "src/ui/bridge/src/DataCleaningService.cpp" "backup/数据功能旧代码/"
cp "src/ui/bridge/src/DataCleaningService_fixed.cpp" "backup/数据功能旧代码/"

# 3. 验证备份完成
ls -la "backup/数据功能旧代码/" | wc -l
# 应该看到12个文件
```

### 任务1.2：验证现有工具类可用性
**目标**：确认QueryBuilder、ConnectionPool等工具类在C++环境中可用  
**预计时间**：1-2小时

#### 验证方法：
1. **检查QueryBuilder接口**：
   ```cpp
   // 创建测试文件：test_querybuilder_availability.cpp
   #include <iostream>
   #include "infrastructure/include/database/QueryBuilder.h"
   
   int main() {
       try {
           // 尝试使用QueryBuilder工厂函数
           auto builder = astock::database::createQueryBuilder(nullptr);
           std::cout << "✅ QueryBuilder可用" << std::endl;
           return 0;
       } catch (const std::exception& e) {
           std::cout << "❌ QueryBuilder不可用: " << e.what() << std::endl;
           return 1;
       }
   }
   ```

2. **检查配置文件**：
   - 确认 `config/database.json` 文件存在且格式正确
   - 确认 `config/cache.json` 文件存在且格式正确

### 任务1.3：从CMakeLists.txt移除旧组件
**目标**：准备编译环境，移除导致链接错误的旧组件  
**预计时间**：1小时

#### 具体步骤：
1. **打开文件**：`src/ui/bridge/src/CMakeLists.txt`
2. **移除以下行**：
   ```cmake
   # 移除这些行：
   DataFetchController.cpp
   DataFetchController_v2.cpp
   DataService_Simple.cpp
   DataCacheService.cpp
   DataCleaningService.cpp
   DataCleaningService_fixed.cpp
   DataSourceManager.cpp
   DatabaseQueryService.cpp
   ```
3. **添加新组件**：
   ```cmake
   # 在target_sources部分添加：
   target_sources(bridge PRIVATE
       # ... 其他现有文件
       DataService.cpp  # 新增
   )
   ```
4. **保存并测试编译**：
   ```bash
   cd "g:/C++/AStockQuantEngine/build"
   cmake --build . --config Debug --target bridge -j 8
   ```

### 任务1.4：创建DataService框架
**目标**：创建极简DataService的.h和.cpp文件框架  
**预计时间**：1小时

#### 具体步骤：
1. **创建头文件**：`src/ui/bridge/include/DataService.h`
   ```cpp
   // DataService.h - 极简接口 (<100行)
   #pragma once
   
   #include <QObject>
   #include <QString>
   #include <QVariantList>
   #include <QVariantMap>
   
   class DataService : public QObject {
       Q_OBJECT
       
   public:
       explicit DataService(QObject* parent = nullptr);
       
       // 核心方法1：查询数据
       Q_INVOKABLE void queryData(const QString& symbol, 
                                 const QString& startDate, 
                                 const QString& endDate);
       
       // 核心方法2：清洗数据
       Q_INVOKABLE void cleanData(const QVariantList& data, 
                                 const QVariantMap& rules);
       
       // 核心方法3：查询并清洗（完整流程）
       Q_INVOKABLE void queryAndCleanData(const QString& symbol,
                                         const QString& startDate,
                                         const QString& endDate,
                                         const QVariantMap& rules);
       
   signals:
       // 极简信号集
       void queryProgress(int progress, const QString& message);
       void queryCompleted(bool success, const QString& message, const QVariantList& data);
       void cleaningProgress(int progress, const QString& message);
       void cleaningCompleted(bool success, const QString& message, const QVariantList& cleanedData);
       void error(const QString& errorMessage);
       
   private:
       // 私有实现方法
       std::shared_ptr<astock::database::QueryBuilder> createQueryBuilder();
       bool checkDatabaseConnection();
       QString generateCacheKey(const QString& symbol, const QString& startDate, const QString& endDate);
   };
   ```

2. **创建源文件框架**：`src/ui/bridge/src/DataService.cpp`
   ```cpp
   // DataService.cpp - 极简实现 (目标: <200行)
   #include "DataService.h"
   #include "infrastructure/include/database/QueryBuilder.h"
   #include <QDebug>
   #include <stdexcept>
   
   DataService::DataService(QObject* parent) : QObject(parent) {
       qDebug() << "DataService: 创建";
   }
   
   // 方法实现占位符
   void DataService::queryData(const QString& symbol, 
                              const QString& startDate, 
                              const QString& endDate) {
       // TODO: 实现查询逻辑
   }
   
   // 其他方法实现...
   ```

## 二、核心实现阶段（第2-4天）

### 任务2.1：实现健壮的queryData方法（第2天）
**目标**：实现健壮的数据查询功能，使用QueryBuilder链式调用  
**预计时间**：1天

#### 实现要求：
1. **健壮性**：捕获所有异常，数据库不可用时程序不崩溃
2. **链式调用**：使用QueryBuilder，禁止SQL字符串拼接
3. **缓存检查**：使用DataServiceCache全局单例
4. **错误处理**：提供有意义的错误信息

#### 代码示例（完整实现）：
```cpp
void DataService::queryData(const QString& symbol, 
                           const QString& startDate, 
                           const QString& endDate) {
    try {
        qDebug() << "DataService::queryData: 开始查询" << symbol << startDate << endDate;
        
        // 1. 参数检查（逻辑判断）
        if (symbol.isEmpty() || startDate.isEmpty() || endDate.isEmpty()) {
            emit error("参数不能为空：symbol, startDate, endDate");
            return;
        }
        
        // 2. 检查数据库连接（逻辑判断，不崩溃）
        if (!checkDatabaseConnection()) {
            emit error("数据库连接不可用，请检查database.json配置");
            return;
        }
        
        // 3. 检查缓存（使用全局缓存单例）
        QString cacheKey = generateCacheKey(symbol, startDate, endDate);
        // TODO: 实现缓存检查
        
        // 4. 执行查询（使用QueryBuilder链式调用）
        auto builder = createQueryBuilder();
        if (!builder) {
            emit error("无法创建QueryBuilder");
            return;
        }
        
        emit queryProgress(10, "开始查询数据库...");
        
        // 链式调用示例
        auto result = builder->from("daily_bar")
                             .select("symbol, trade_date, open, high, low, close, volume")
                             .where("symbol", ConditionType::COND_EQUAL, symbol)
                             .andWhere("trade_date", ConditionType::COND_BETWEEN, startDate, endDate)
                             .execute();
        
        emit queryProgress(90, "查询完成，处理结果...");
        
        // 5. 缓存结果
        // TODO: 实现缓存存储
        
        // 6. 发送完成信号
        emit queryCompleted(true, QString("查询成功，获取%1条数据").arg(result.size()), result);
        
    } catch (const std::exception& e) {
        QString errorMsg = QString("查询失败: %1").arg(e.what());
        qCritical() << "DataService::queryData:" << errorMsg;
        emit error(errorMsg);
    } catch (...) {
        QString errorMsg = "未知错误，查询失败";
        qCritical() << "DataService::queryData:" << errorMsg;
        emit error(errorMsg);
    }
}
```

### 任务2.2：实现cleanData方法（第3天）
**目标**：实现数据清洗功能，使用现有DataCleaningEngine  
**预计时间**：0.5天

#### 实现要求：
1. **使用现有引擎**：直接使用DataCleaningEngine，不创建新的清洗逻辑
2. **规则转换**：简单的规则格式转换
3. **错误处理**：捕获清洗过程中的异常

### 任务2.3：实现queryAndCleanData方法（第3天）
**目标**：实现完整的查询+清洗流程  
**预计时间**：0.5天

#### 实现要求：
1. **流程组合**：组合queryData和cleanData的功能
2. **进度反馈**：提供完整的进度反馈
3. **错误处理**：处理流程中的任何错误

### 任务2.4：更新QML类型注册（第4天）
**目标**：更新registerQmlTypes.cpp，注册新的DataService  
**预计时间**：1小时

#### 具体步骤：
1. **打开文件**：`src/ui/bridge/src/registerQmlTypes.cpp`
2. **添加注册代码**：
   ```cpp
   #include "DataService.h"
   
   void registerQmlTypes() {
       // ... 现有注册代码
       
       // 注册新的DataService
       qmlRegisterType<DataService>("AStock.Bridge", 1, 0, "DataService");
       
       qDebug() << "✅ DataService已注册到QML";
   }
   ```

### 任务2.5：更新UI界面（第4天）
**目标**：更新DataSourceModal.qml和DataCleaningModal.qml使用新接口  
**预计时间**：2-3小时

#### 具体步骤：
1. **更新DataSourceModal.qml**：
   ```qml
   // 将原来的 DataFetchController_v2 替换为 DataService
   DataService {
       id: dataService
       
       onQueryCompleted: {
           // 处理查询完成
           if (success) {
               console.log("查询成功:", message, "数据量:", data.length)
               // 保存数据，准备进入清洗界面
           } else {
               console.error("查询失败:", message)
           }
       }
       
       onError: {
           console.error("数据服务错误:", errorMessage)
           // 显示错误信息给用户
       }
   }
   ```

2. **更新DataCleaningModal.qml**：
   ```qml
   // 使用新的清洗接口
   dataService.cleanData(rawData, cleaningRules)
   ```

## 三、测试与优化阶段（第5-7天）

### 任务3.1：单元测试（第5天）
**目标**：为DataService创建基本单元测试  
**预计时间**：1天

#### 测试内容：
1. **正常流程测试**：
   ```cpp
   TEST(DataService, QueryData_Normal) {
       DataService service;
       // 测试正常查询
   }
   ```

2. **错误场景测试**：
   ```cpp
   TEST(DataService, QueryData_EmptyParams) {
       DataService service;
       // 测试空参数
   }
   
   TEST(DataService, QueryData_DatabaseUnavailable) {
       DataService service;
       // 测试数据库不可用
   }
   ```

### 任务3.2：集成测试（第6天）
**目标**：测试完整的数据源添加→查询→清洗流程  
**预计时间**：1天

#### 测试场景：
1. **完整工作流测试**：
   ```cpp
   void testFullWorkflow() {
       // 1. 配置数据源
       // 2. 查询数据
       // 3. 配置清洗规则
       // 4. 执行清洗
       // 5. 验证结果
   }
   ```

2. **性能测试**：
   ```cpp
   void testPerformance() {
       // 测试查询响应时间 < 3秒
       // 测试清洗速度 > 1000条/秒
   }
   ```

### 任务3.3：健壮性测试（第6天）
**目标**：测试数据库不可用等异常情况  
**预计时间**：0.5天

#### 测试场景：
1. **数据库连接失败测试**
2. **网络异常测试**
3. **内存压力测试**

### 任务3.4：代码审查与优化（第7天）
**目标**：确保代码符合设计原则，进行最终优化  
**预计时间**：1天

#### 审查清单：
- [ ] DataService代码是否超过300行？
- [ ] 是否有SQL字符串拼接？
- [ ] 数据库连接失败时程序是否会崩溃？
- [ ] 错误处理是否完善？
- [ ] 是否使用了QueryBuilder链式调用？
- [ ] 是否使用了全局缓存单例？

## 四、每日检查点

### 第1天结束检查：
- [ ] 代码备份完成
- [ ] CMakeLists.txt已更新
- [ ] DataService框架创建完成
- [ ] 项目可以编译（可能有链接错误，但语法正确）

### 第2天结束检查：
- [ ] queryData方法实现完成
- [ ] 健壮性测试通过（数据库不可用时程序不崩溃）
- [ ] 使用QueryBuilder链式调用

### 第3天结束检查：
- [ ] cleanData方法实现完成
- [ ] queryAndCleanData方法实现完成
- [ ] 所有核心功能实现

### 第4天结束检查：
- [ ] QML类型注册更新
- [ ] UI界面更新完成
- [ ] 完整流程可以运行

### 第5天结束检查：
- [ ] 单元测试完成
- [ ] 代码覆盖率达标

### 第6天结束检查：
- [ ] 集成测试通过
- [ ] 健壮性测试通过
- [ ] 性能测试达标

### 第7天结束检查：
- [ ] 代码审查通过
- [ ] 所有检查清单项目通过
- [ ] 项目可以完整编译和运行

## 五、风险管理

### 技术风险：
1. **现有工具类不兼容**
   - **应对**：创建简单的适配层或直接修改工具类使用方式
   - **检测**：第1天的验证步骤中发现

2. **配置文件格式问题**
   - **应对**：创建配置文件模板和验证工具
   - **检测**：第1天的配置检查中发现

### 时间风险：
1. **实现时间超出预期**
   - **应对**：优先实现核心功能，简化非核心功能
   - **检测**：每天结束时评估进度

### 质量风险：
1. **健壮性不足**
   - **应对**：严格遵循健壮性原则，加强异常处理测试
   - **检测**：第6天的健壮性测试中发现

## 六、成功标准（最终验收）

### 功能标准：
- [ ] 项目编译通过（零链接错误）
- [ ] 数据源添加功能完整可用
- [ ] 数据查询功能完整可用（使用QueryBuilder链式调用）
- [ ] 数据清洗功能完整可用
- [ ] 数据库不可用时程序不崩溃，提供有意义的错误信息

### 代码质量标准：
- [ ] DataService代码不超过300行
- [ ] 无SQL字符串拼接
- [ ] 无过度设计，每个方法只做一件事
- [ ] 完善的错误处理，无未捕获异常

### 性能标准：
- [ ] 数据查询响应时间 < 3秒（本地数据库）
- [ ] 清洗处理不阻塞UI
- [ ] 内存使用稳定

## 七、立即开始

### 今天可以开始的步骤：
1. **创建备份目录**（5分钟）
2. **备份旧代码**（15分钟）
3. **验证配置文件**（10分钟）
4. **更新CMakeLists.txt**（30分钟）

### 明天开始核心实现：
1. **实现queryData方法**（重点：健壮性和链式调用）
2. **测试数据库不可用场景**
3. **验证缓存功能**

---

**文档版本**：1.0  
**创建日期**：2026年2月24日  
**适用对象**：实施重构的开发人员  
**预计工期**：5-7个工作日  
**关键成功因素**：严格遵守设计原则，每天验证进度，及时纠正跑偏