# 数据源添加与清洗功能彻底重构方案

## 一、重构决策背景

### 1.1 当前架构问题总结
经过深入分析，当前数据源添加和清洗功能的架构存在以下根本问题：

1. **过度复杂的设计**：DataFetchController_v2支持双后端模式、自动服务选择、多种查询服务类型，导致代码维护困难
2. **职责不清**：单个控制器承担数据查询、清洗、缓存管理、UI协调等多重职责
3. **接口混乱**：大量QML属性、信号、槽函数交织，难以理解和维护
4. **实现不完整**：存在大量简化实现和占位符代码，功能不完整
5. **编译错误**：65个链接错误，项目无法编译
6. **违背用户原则**：未充分使用现有工具类，存在SQL字符串拼接

### 1.2 重构核心目标
1. **彻底简化**：删除当前复杂架构，从头开始设计
2. **功能齐全**：保持完整的数据源添加和清洗功能
3. **精简实用**：每个组件只做一件事，不做过度抽象
4. **使用现有工具**：优先使用QueryBuilder链式调用、全局缓存系统等现有工具
5. **配置驱动**：数据库连接信息从配置文件读取，业务代码中不出现

## 二、重构范围与原则

### 2.1 需要删除的组件
以下组件需要完全删除，因为它们属于当前复杂架构的一部分：

1. **DataFetchController_v2**（完全删除）
2. **DataService_Simple**（功能重复，使用QueryBuilder替代）
3. **DatabaseQueryService**（过度设计，功能重复）
4. **DataSourceManager**（职责不清）
5. **DataCacheService**（重复实现，使用全局缓存单例）

### 2.2 保留的组件
以下组件需要保留，因为它们是有用的基础设施：

1. **QueryBuilder**（链式查询接口，必须使用）
2. **ConnectionPool**（数据库连接池，全局基础设施）
3. **DataServiceCache**（全局缓存单例）
4. **DataCleaningEngine**（数据清洗引擎）
5. **现有UI界面**：DataSourceModal.qml、DataCleaningModal.qml（只更新后端接口）

### 2.3 重构设计原则
1. **KISS原则**（Keep It Simple, Stupid）：最简单的解决方案就是最好的
2. **单一职责**：每个类只做一件事
3. **使用现有工具**：不重复造轮子
4. **配置驱动**：所有配置从配置文件读取
5. **链式调用**：数据库查询必须使用QueryBuilder链式接口

## 三、全新架构设计

### 3.1 简洁架构
```
┌─────────────────────┐     ┌─────────────────────┐
│     UI层 (QML)      │     │  配置文件管理      │
│ • DataSourceModal   │     │ • database.json    │
│ • DataCleaningModal │     │ • cache.json       │
└──────────┬──────────┘     └──────────┬──────────┘
           │                            │
           ▼                            ▼
┌─────────────────────────────────────────────────┐
│            DataService（新建，极简版）           │
│  核心职责：提供数据查询和清洗的统一接口         │
└──────────┬──────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────┐
│             现有工具类层                         │
│ • QueryBuilder（链式查询）                       │
│ • ConnectionPool（数据库连接池）                 │
│ • DataServiceCache（全局缓存）                   │
│ • DataCleaningEngine（数据清洗）                 │
└─────────────────────────────────────────────────┘
```

### 3.2 核心组件设计

#### 1. **DataService（新建，极简版）**
**唯一职责**：提供数据查询和清洗的简单接口
**设计要点**：
- 只有3-5个核心方法
- 使用现有的QueryBuilder、ConnectionPool等工具
- 不管理复杂状态，只提供功能

```cpp
// 极简接口设计
class DataService : public QObject {
    Q_OBJECT
    
public:
    explicit DataService(QObject* parent = nullptr);
    
    // 核心方法1：查询数据
    Q_INVOKABLE void queryData(const QString& symbol, 
                              const QString& startDate, 
                              const QString& endDate);
    
    // 核心方法2：清洗数据
    Q_INVOKABLE void cleanData(const QVariantList& data, 
                              const QVariantMap& rules);
    
    // 核心方法3：查询并清洗（完整流程）
    Q_INVOKABLE void queryAndCleanData(const QString& symbol,
                                      const QString& startDate,
                                      const QString& endDate,
                                      const QVariantMap& rules);
    
signals:
    // 极简信号集
    void queryProgress(int progress, const QString& message);
    void queryCompleted(bool success, const QString& message, const QVariantList& data);
    void cleaningProgress(int progress, const QString& message);
    void cleaningCompleted(bool success, const QString& message, const QVariantList& cleanedData);
    void error(const QString& errorMessage);
    
private:
    // 使用现有工具类
    std::shared_ptr<astock::database::QueryBuilder> createQueryBuilder();
    DataServiceCache& m_cache = DataServiceCache::getInstance();
    std::unique_ptr<DataCleaningEngine> m_cleaningEngine;
};
```

#### 2. **QueryBuilder使用规范**
**强制要求**：所有数据库查询必须使用链式调用
```cpp
// 正确：使用链式调用
auto builder = createQueryBuilder();
auto result = builder->from("daily_bar")
                     .select("symbol, trade_date, open, high, low, close, volume")
                     .where("symbol", ConditionType::COND_EQUAL, symbol)
                     .andWhere("trade_date", ConditionType::COND_BETWEEN, startDate, endDate)
                     .execute();

// 错误：使用SQL字符串拼接（禁止）
QString sql = QString("SELECT * FROM daily_bar WHERE symbol = '%1'").arg(symbol);
```

#### 3. **数据库连接管理**
**原则**：连接信息在配置文件中，主程序统一初始化
```cpp
// 主程序初始化（main.cpp或AppController中）
void initializeDatabase() {
    // 1. 从配置文件读取连接信息
    auto config = loadConfig("config/database.json");
    
    // 2. 初始化全局连接池
    auto pool = astock::database::ConnectionPool::create(config);
    pool->initialize();
    
    // 3. 后续所有数据库操作都通过QueryBuilder工厂函数
    //    业务代码中不出现数据库连接信息
}
```

#### 4. **缓存使用规范**
**原则**：使用现有的全局缓存单例
```cpp
// 统一缓存使用方式
DataServiceCache& cache = DataServiceCache::getInstance();

// 检查缓存
QString cacheKey = generateCacheKey(symbol, startDate, endDate);
if (auto cachedData = cache.get(cacheKey)) {
    return cachedData;
}

// 存储缓存
cache.put(cacheKey, data, getCacheExpiryTime());
```

## 四、彻底重构实施步骤

### 阶段一：准备工作（1天）

#### 步骤1.1：备份当前代码
```bash
# 创建备份目录
mkdir -p backup/数据功能旧代码

# 备份相关文件
cp src/ui/bridge/include/DataFetchController_v2.h backup/
cp src/ui/bridge/src/DataFetchController_v2.cpp backup/
cp src/ui/bridge/include/DataService_Simple.h backup/
cp src/ui/bridge/src/DataService_Simple.cpp backup/
# ... 备份其他相关文件
```

#### 步骤1.2：从CMakeLists.txt中移除旧组件
```cmake
# 修改 src/ui/bridge/src/CMakeLists.txt
# 移除以下行：
# DataFetchController.cpp
# DataFetchController_v2.cpp
# DataService_Simple.cpp
# DataCacheService.cpp
# DataCleaningService.cpp
# DataCleaningService_fixed.cpp
# DataSourceManager.cpp
# DatabaseQueryService.cpp

# 添加新组件：
target_sources(bridge PRIVATE
    # ... 其他文件
    DataService.cpp  # 新建
)
```

#### 步骤1.3：验证现有工具类可用性
```cpp
// 创建验证测试
void verifyTools() {
    // 1. 验证QueryBuilder可用性
    testQueryBuilder();
    
    // 2. 验证ConnectionPool可用性
    testConnectionPool();
    
    // 3. 验证DataServiceCache可用性
    testCache();
    
    // 4. 验证DataCleaningEngine可用性
    testCleaningEngine();
}
```

### 阶段二：实现新架构（2-3天）

#### 步骤2.1：创建极简DataService
```cpp
// 创建文件：src/ui/bridge/include/DataService.h
// 创建文件：src/ui/bridge/src/DataService.cpp

// 实现要求：
// 1. 不超过300行代码
// 2. 只实现核心功能
// 3. 使用现有工具类
// 4. 完善的错误处理
```

#### 步骤2.2：更新UI界面使用新接口
```qml
// 更新 DataSourceModal.qml
// 将原来的 DataFetchController_v2 替换为 DataService

// 更新 DataCleaningModal.qml
// 使用新的清洗接口
```

#### 步骤2.3：实现配置管理
```cpp
// 创建配置文件读取工具
class ConfigManager {
public:
    static DatabaseConfig loadDatabaseConfig(const QString& path);
    static CacheConfig loadCacheConfig(const QString& path);
};
```

### 阶段三：测试与优化（1-2天）

#### 步骤3.1：单元测试
```cpp
// 为DataService创建单元测试
TEST(DataService, QueryData) { ... }
TEST(DataService, CleanData) { ... }
TEST(DataService, QueryAndCleanData) { ... }
```

#### 步骤3.2：集成测试
```cpp
// 测试完整流程：数据源添加 → 查询 → 清洗
void testFullWorkflow() {
    // 1. 配置数据源
    // 2. 查询数据
    // 3. 配置清洗规则
    // 4. 执行清洗
    // 5. 验证结果
}
```

#### 步骤3.3：性能测试
```cpp
// 验证性能要求
void testPerformance() {
    // 1. 查询响应时间 < 3秒
    // 2. 清洗速度 > 1000条/秒
    // 3. 内存使用稳定
}
```

## 五、代码删除清单

### 5.1 必须删除的文件
```
src/ui/bridge/include/DataFetchController_v2.h
src/ui/bridge/src/DataFetchController_v2.cpp
src/ui/bridge/include/DataService_Simple.h
src/ui/bridge/src/DataService_Simple.cpp
src/ui/bridge/include/DatabaseQueryService.h
src/ui/bridge/src/DatabaseQueryService.cpp
src/ui/bridge/include/DataCacheService.h
src/ui/bridge/src/DataCacheService.cpp
src/ui/bridge/include/DataSourceManager.h
src/ui/bridge/src/DataSourceManager.cpp
src/ui/bridge/src/DataCleaningService.cpp
src/ui/bridge/src/DataCleaningService_fixed.cpp
```

### 5.2 需要更新的文件
```
src/ui/bridge/src/CMakeLists.txt
src/ui/bridge/src/registerQmlTypes.cpp  # 更新QML类型注册
```

### 5.3 需要检查的依赖
检查以下文件是否引用了被删除的组件，并进行更新：
```
src/app/Qml/components/DataAnalysis/DataSourceModal.qml
src/app/Qml/components/DataAnalysis/DataCleaningModal.qml
src/app/Qml/page/DataAnalysis/Datamain.qml
其他可能引用DataFetchController_v2的QML文件
```

## 六、风险与应对

### 6.1 技术风险
**风险**：现有工具类接口可能发生变化
**应对**：先验证工具类可用性，创建兼容性适配层（如果需要）

### 6.2 集成风险
**风险**：新组件与现有系统集成困难
**应对**：保持简单接口，逐步替换，提供回滚方案

### 6.3 时间风险
**风险**：重构时间超过预期
**应对**：分阶段实施，先实现核心功能，再逐步完善

## 七、成功标准

### 7.1 功能标准
1. ✅ 项目可以编译通过（零链接错误）
2. ✅ 数据源添加功能完整可用
3. ✅ 数据查询使用QueryBuilder链式调用
4. ✅ 数据清洗功能完整可用
5. ✅ 缓存功能正常工作
6. ✅ UI界面响应正常

### 7.2 代码质量标准
1. ✅ DataService代码不超过300行
2. ✅ 无SQL字符串拼接
3. ✅ 数据库连接信息不在业务代码中出现
4. ✅ 使用全局缓存单例
5. ✅ 完善的错误处理

### 7.3 性能标准
1. ✅ 数据查询响应时间 < 3秒
2. ✅ 清洗处理不阻塞UI
3. ✅ 内存使用稳定

## 八、重构后的收益

### 8.1 代码质量提升
- **代码行数减少**：预计从2000+行减少到300行左右
- **可维护性提高**：单一职责，结构清晰
- **可测试性提高**：接口简单，易于测试

### 8.2 开发效率提升
- **理解成本降低**：新开发者可以快速理解架构
- **修改成本降低**：功能隔离，修改影响范围小
- **调试效率提高**：问题定位更容易

### 8.3 系统稳定性提升
- **编译稳定性**：消除链接错误
- **运行稳定性**：完善的错误处理
- **性能稳定性**：使用成熟工具类，性能可预测

## 九、立即行动建议

### 9.1 今天（准备阶段）
1. **创建代码备份**：备份所有将被删除的文件
2. **验证工具类**：测试QueryBuilder、ConnectionPool等工具类的可用性
3. **检查配置文件**：确认database.json、cache.json等配置文件的格式和位置

### 9.2 明天（开始实施）
1. **开始阶段一**：从CMakeLists.txt中移除旧组件
2. **创建DataService框架**：创建.h和.cpp文件框架
3. **更新QML类型注册**：准备注册新的DataService

### 9.3 后续计划
1. **按阶段实施**：严格按照三个阶段计划执行
2. **每日验证**：每天结束时确保项目可编译
3. **及时沟通**：遇到问题及时讨论解决方案

---

**文档版本**：1.0  
**制定日期**：2026年2月24日  
**制定原则**：彻底重构，从头开始，简洁实用，使用现有工具  
**核心目标**：创建一个不超过300行的DataService，提供完整的数据查询和清洗功能  
**成功标志**：项目可编译，功能完整，代码简洁