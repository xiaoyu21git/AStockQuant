# 数据清洗与缓存功能设计方案

## 一、UI功能分析

### 1. DataSourceModal.qml（添加数据源）
- **功能**：选择数据提供商、交易所、时间范围、股票代码、数据类型
- **操作流程**：
  1. 用户填写表单
  2. 点击"测试连接" → 测试数据库连接
  3. 点击"加载预览" → 查询数据库并缓存数据
  4. 点击"添加" → 保存数据源配置并启动数据加载
- **核心需求**：查询数据库，将数据添加到缓存

### 2. DataCleaningModal.qml（数据清洗）
- **功能**：设置清洗规则、预览清洗效果、执行数据清洗
- **操作流程**：
  1. 用户设置清洗规则（如价格范围、成交量过滤等）
  2. 点击"预览清洗效果" → 预览清洗结果（不实际修改数据）
  3. 点击"执行数据清洗" → 执行异步清洗操作
  4. 查看清洗统计（原始数据、筛选后数据、清洗后数据、处理时间）
- **核心需求**：根据规则清洗数据，支持预览和完整清洗

## 二、现有组件分析

### 已实现功能：
1. **DataService_Simple** - 极简版数据服务 ✅
   - 核心功能：查询数据库
   - 测试通过：数据库连接和查询功能正常

2. **现有缓存系统** - DataServiceCache.h ✅
   - 已有完整的缓存接口
   - 支持数据存储、获取、统计、清理

3. **现有清洗引擎** - DataCleaningEngine.h ✅
   - 已有数据清洗引擎框架
   - 支持规则定义和清洗逻辑

## 三、类分工设计方案

### 原则：
1. **单一职责**：每个类只负责一个核心功能
2. **线程策略**：耗时操作使用线程，UI响应保持流畅
3. **真实验证**：每个功能完成后进行真实数据验证
4. **清晰边界**：不同功能使用不同类管理

### 类设计：

#### 1. **DatabaseQueryService**（新类）
- **职责**：专门负责数据库查询
- **继承关系**：从DataService_Simple简化而来
- **线程使用**：所有查询操作都在工作线程执行
- **接口**：
  ```cpp
  // 异步查询日线数据
  void queryDailyDataAsync(const QString& symbol, 
                          const QString& startDate, 
                          const QString& endDate);
  
  // 异步查询多symbol数据
  void queryMultipleSymbolsAsync(const QStringList& symbols,
                                const QString& startDate,
                                const QString& endDate);
  ```

#### 2. **DataCacheService**（新类）
- **职责**：专门负责数据缓存
- **依赖**：使用现有的DataServiceCache.h
- **线程使用**：缓存操作在查询线程中执行（不额外创建线程）
- **接口**：
  ```cpp
  // 缓存数据
  void cacheData(const QString& cacheKey, const QVariantList& data);
  
  // 获取缓存数据
  bool getCachedData(const QString& cacheKey, QVariantList& result);
  
  // 缓存统计
  CacheStats getCacheStats() const;
  ```

#### 3. **DataCleaningService**（新类）
- **职责**：专门负责数据清洗
- **依赖**：使用现有的DataCleaningEngine.h
- **线程使用**：清洗预览（同步），完整清洗（异步线程）
- **接口**：
  ```cpp
  // 预览清洗效果（同步，适合小数据量）
  QVariantList previewCleaning(const QVariantList& data, 
                              const QVariantMap& rules);
  
  // 执行完整清洗（异步，适合大数据量）
  void executeCleaningAsync(const QVariantList& data,
                           const QVariantMap& rules);
  ```

#### 4. **DataSourceManager**（新类）
- **职责**：管理数据源配置和加载流程
- **线程使用**：协调各个服务，管理异步操作流程
- **流程控制**：
  1. 接收UI请求（添加数据源）
  2. 调用DatabaseQueryService查询数据
  3. 调用DataCacheService缓存数据
  4. 通知UI加载完成

## 四、线程使用策略

### 需要线程的操作：
1. **数据库查询**（DatabaseQueryService）
   - 原因：网络I/O和数据库I/O耗时
   - 策略：使用Qt的QThreadPool或std::thread

2. **完整数据清洗**（DataCleaningService）
   - 原因：大数据量清洗计算耗时
   - 策略：使用异步线程，支持进度报告

3. **批量数据缓存**（DataCacheService）
   - 原因：大量数据序列化/反序列化耗时
   - 策略：在查询线程中直接执行（已经是后台线程）

### 不需要线程的操作：
1. **清洗预览**（DataCleaningService）
   - 原因：小数据量快速计算
   - 策略：同步执行，立即返回结果

2. **缓存读取**（DataCacheService）
   - 原因：内存操作速度快
   - 策略：同步执行

3. **数据源配置管理**（DataSourceManager）
   - 原因：配置操作轻量
   - 策略：同步执行

## 五、验证方案

### 验证原则：
1. **真实数据**：使用真实数据库中的数据
2. **完整流程**：验证从UI点击到功能完成的完整流程
3. **结果验证**：验证功能结果的正确性
4. **性能验证**：验证耗时操作的性能

### 验证步骤：

#### 1. 数据库查询功能验证 ✅
- [x] 已验证：test_dataservice_simple.py 测试通过
- [x] 结果：能够查询真实数据库，获取22条记录

#### 2. 数据缓存功能验证
- [ ] 创建验证脚本：test_cache_service.py
- [ ] 验证内容：
  - 数据存储到缓存
  - 从缓存读取数据
  - 缓存过期机制
  - 缓存清理功能

#### 3. 数据清洗功能验证
- [ ] 创建验证脚本：test_cleaning_service.py
- [ ] 验证内容：
  - 清洗规则定义
  - 预览清洗效果（同步）
  - 完整清洗执行（异步）
  - 清洗结果验证

#### 4. 完整流程验证
- [ ] 创建验证脚本：test_full_workflow.py
- [ ] 验证内容：
  - 从UI模拟点击"添加数据源"
  - 完整执行：查询 → 缓存 → 清洗预览
  - 验证最终结果

## 六、实施计划

### 第一阶段：创建基础服务类（1-2天）
1. 创建DatabaseQueryService（基于DataService_Simple）
2. 创建DataCacheService（基于DataServiceCache）
3. 创建基础测试验证

### 第二阶段：创建清洗服务类（1-2天）
1. 创建DataCleaningService（基于DataCleaningEngine）
2. 实现同步预览和异步清洗
3. 创建清洗测试验证

### 第三阶段：创建流程管理器（1天）
1. 创建DataSourceManager
2. 集成三个服务
3. 实现完整的添加数据源流程

### 第四阶段：UI集成测试（1天）
1. 更新DataFetchController使用新服务
2. 测试完整UI流程
3. 性能优化和调试

## 七、与现有代码的兼容性

### 保持兼容的组件：
1. **DataFetchController**：继续作为QML桥接
2. **DataServiceCache**：直接使用现有缓存系统
3. **DataCleaningEngine**：直接使用现有清洗引擎
4. **ConnectionPool**：使用现有数据库连接池

### 需要简化的组件：
1. **DataService**：过于复杂，建议逐步废弃
2. **DataService_Simple**：作为DatabaseQueryService的基础

## 八、风险与应对

### 风险1：现有组件不完整
- **应对**：先验证现有组件的可用性，必要时补充缺失功能

### 风险2：线程同步问题
- **应对**：使用Qt的信号槽机制进行线程间通信，避免直接共享数据

### 风险3：性能问题
- **应对**：实施渐进式优化，先保证功能正确性，再优化性能

### 风险4：UI响应延迟
- **应对**：确保耗时操作都在后台线程，UI线程只处理轻量操作

## 九、成功标准

### 功能标准：
1. ✅ 能够查询真实数据库数据
2. ✅ 能够将查询结果缓存
3. ✅ 能够设置和预览清洗规则
4. ✅ 能够执行异步数据清洗
5. ✅ UI操作响应流畅

### 性能标准：
1. 数据库查询响应时间 < 5秒（本地数据库）
2. 数据缓存操作时间 < 1秒（1000条记录）
3. 清洗预览响应时间 < 2秒（1000条记录）
4. 完整清洗进度可报告，不阻塞UI

### 质量标准：
1. 每个功能有独立的验证测试
2. 测试使用真实数据，不使用模拟数据
3. 错误处理完善，不会崩溃
4. 代码结构清晰，易于维护