# 数据查询与清洗功能重构总结与方案

## 一、当前项目状态总结

### 1.1 核心问题概述
基于之前的分析，项目面临以下主要问题：
- **编译链接错误**：65个无法解析的外部符号，项目无法编译
- **架构混乱**：多个数据服务并存，职责不清，接口不一致
- **实现不完整**：大量简化实现和占位符代码
- **类型系统混乱**：枚举类型和常量定义重复不一致

### 1.2 现有优势与基础
- **完善的UI设计**：DataSourceModal.qml和DataCleaningModal.qml设计完整
- **基础工具类完备**：Foundation库、Engine库、Infrastructure库提供丰富工具
- **链式查询接口**：现有的QueryBuilder提供链式调用接口
- **全局缓存系统**：DataServiceCache已在项目中实现并全局有效
- **配置文件支持**：缓存和数据配置已有配置文件

## 二、用户要求澄清与设计原则

### 2.1 核心要求澄清
1. **数据库信息封装**：数据库连接信息不应在业务代码中出现，应与主程序一起初始化
2. **使用链式查询接口**：必须使用现有的QueryBuilder链式调用，避免字符串拼接SQL
3. **精简实用设计**：避免过度设计，但要功能齐全、专业实用
4. **全局缓存系统**：缓存系统是整个程序的基础设施，不应局限于数据清洗部分

### 2.2 新设计原则
1. **优先使用现有工具类**：充分利用QueryBuilder、ConnectionPool、DataServiceCache等现有工具
2. **精简实用**：每个类只做一件事，不做过度抽象
3. **配置驱动**：使用配置文件管理数据库连接和缓存配置
4. **全局基础设施**：数据库连接池和缓存系统作为全局基础设施
5. **链式调用**：所有数据库查询使用QueryBuilder链式接口

## 三、基于现有工具的简化架构

### 3.1 架构设计目标
```
简化目标：UI → 控制器 → 服务 → 现有工具类
避免创建过多抽象层，直接使用现有工具
```

### 3.2 核心组件职责

#### 1. **DataQueryService（基于现有QueryBuilder）**
**职责**：专门负责数据库查询，使用链式调用
**不创建新工具类**：只是封装现有QueryBuilder的使用
```cpp
// 简化示例
class DataQueryService {
    // 使用现有的QueryBuilder工厂函数
    std::unique_ptr<astock::database::QueryBuilder> createBuilder() {
        return astock::database::createQueryBuilder();
    }
    
    // 链式调用示例
    QVariantList queryDailyData(const QString& symbol, 
                               const QString& startDate, 
                               const QString& endDate) {
        auto builder = createBuilder();
        return builder->from("daily_bar")
                     .select("symbol, trade_date, open, high, low, close, volume")
                     .where("symbol", ConditionType::COND_EQUAL, symbol)
                     .andWhere("trade_date", ConditionType::COND_BETWEEN, startDate, endDate)
                     .execute();
    }
};
```

#### 2. **DataCleaningService（基于现有DataCleaningEngine）**
**职责**：专门负责数据清洗
**不创建新引擎**：直接使用现有的DataCleaningEngine
```cpp
// 简化示例
class DataCleaningService {
    std::unique_ptr<DataCleaningEngine> m_engine;
    
    QVariantList cleanData(const QVariantList& data, const QVariantMap& rules) {
        // 转换规则格式（简化转换，不创建复杂抽象）
        auto cleaningRules = convertRulesSimply(rules);
        return m_engine->cleanData(data, cleaningRules);
    }
};
```

#### 3. **DataFlowCoordinator（新建，极简版）**
**职责**：协调数据查询和清洗流程
**极简设计**：只做流程协调，不做复杂状态管理
```cpp
// 极简示例
class DataFlowCoordinator : public QObject {
    Q_OBJECT
    
private:
    std::shared_ptr<DataQueryService> m_queryService;
    std::shared_ptr<DataCleaningService> m_cleaningService;
    // 使用全局缓存单例
    DataServiceCache& m_cache = DataServiceCache::getInstance();
    
public:
    // 核心方法：执行完整的数据加载和清洗流程
    void loadAndCleanData(const QString& symbol, 
                         const QString& startDate, 
                         const QString& endDate,
                         const QVariantMap& cleaningRules) {
        // 1. 检查缓存（使用全局缓存）
        QString cacheKey = generateCacheKey(symbol, startDate, endDate, cleaningRules);
        if (auto cached = m_cache.get(cacheKey)) {
            emit dataReady(cached);
            return;
        }
        
        // 2. 查询数据（使用链式查询）
        auto rawData = m_queryService->queryDailyData(symbol, startDate, endDate);
        
        // 3. 清洗数据
        auto cleanedData = m_cleaningService->cleanData(rawData, cleaningRules);
        
        // 4. 缓存结果
        m_cache.put(cacheKey, cleanedData);
        
        // 5. 发送完成信号
        emit dataReady(cleanedData);
    }
};
```

### 3.3 数据库连接管理
**原则**：数据库连接信息从配置文件读取，在主程序初始化
```cpp
// 主程序初始化
int main() {
    // 1. 从配置文件读取数据库连接信息
    auto dbConfig = loadDatabaseConfig("config/database.json");
    
    // 2. 初始化全局连接池
    auto connectionPool = astock::database::ConnectionPool::create(dbConfig);
    connectionPool->initialize();
    
    // 3. 在需要使用数据库的地方，通过工厂函数获取QueryBuilder
    // 业务代码中不出现数据库连接信息
}
```

## 四、现有问题与解决方案对应表

### 4.1 编译链接问题
| 问题 | 文件 | 解决方案 |
|------|------|----------|
| DataCacheService.cpp实现不完整 | DataCacheService.cpp | 直接使用现有的DataServiceCache单例，不重复实现 |
| 函数签名不匹配 | DataCleaningService_fixed.cpp | 统一使用正确的函数签名，避免类型转换错误 |
| ConnectionPool单例方法不存在 | DataService_Simple.cpp | 使用正确的ConnectionPool工厂函数 |

### 4.2 架构设计问题
| 问题 | 现状 | 解决方案 |
|------|------|----------|
| 多个数据服务并存 | DataService、DataService_Simple、DatabaseQueryService | 统一为DataQueryService，使用现有QueryBuilder |
| 简化实现过多 | QTimer模拟查询，直接返回原始数据 | 实现真实功能，使用链式查询 |
| 线程管理混乱 | QThreadPool与自定义线程混用 | 统一使用Foundation线程池 |

### 4.3 代码质量问题
| 问题 | 影响 | 解决方案 |
|------|------|----------|
| 数据库信息泄露 | 业务代码中出现数据库连接信息 | 通过配置文件管理，主程序统一初始化 |
| SQL字符串拼接 | 容易出错，不安全 | 使用QueryBuilder链式调用 |
| 错误处理不完善 | 系统不稳定 | 完善异常处理和恢复机制 |

## 五、精简实施计划

### 5.1 第一阶段：修复编译，统一基础（2-3天）
**目标**：项目可编译，基础组件可用

1. **修复编译错误**（1天）
   - 统一DataSourceType枚举定义
   - 修正函数签名不匹配
   - 确保DataServiceCache单例可访问

2. **创建精简DataQueryService**（1天）
   - 基于现有QueryBuilder
   - 提供统一的链式查询接口
   - 移除所有SQL字符串拼接

3. **完善DataCleaningService**（1天）
   - 基于现有DataCleaningEngine
   - 修复规则转换函数
   - 完善错误处理

### 5.2 第二阶段：整合流程，更新UI（2-3天）
**目标**：完整的数据加载和清洗流程可工作

1. **创建DataFlowCoordinator**（1天）
   - 极简流程协调
   - 集成缓存检查
   - 提供统一信号接口

2. **更新UI界面使用新接口**（1天）
   - DataSourceModal.qml使用新接口
   - DataCleaningModal.qml使用新接口
   - 添加基本进度反馈

3. **端到端测试**（1天）
   - 验证完整流程
   - 测试错误处理
   - 性能基本验证

### 5.3 第三阶段：优化完善（1-2天）
**目标**：提升稳定性和用户体验

1. **错误处理完善**（0.5天）
2. **性能优化**（0.5天）
3. **文档和示例**（1天）

## 六、关键设计决策说明

### 6.1 为什么选择极简设计？
- **避免过度设计**：现有代码已经过度抽象，需要简化
- **利用现有工具**：QueryBuilder、DataCleaningEngine等工具类已经成熟
- **快速见效**：先让功能可用，再逐步优化

### 6.2 如何处理数据库连接？
- **配置文件管理**：database.json配置文件
- **主程序初始化**：程序启动时初始化连接池
- **工厂函数获取**：业务代码通过工厂函数获取QueryBuilder实例
- **无连接信息泄露**：业务代码中看不到数据库连接字符串

### 6.3 如何保证缓存一致性？
- **使用全局单例**：DataServiceCache::getInstance()
- **统一的缓存键生成**：基于请求参数哈希
- **配置驱动过期策略**：通过配置文件管理缓存策略

### 6.4 如何处理线程安全？
- **使用Foundation线程池**：统一线程管理
- **Qt信号槽机制**：线程间通信
- **简单的锁策略**：只在必要时使用互斥锁

## 七、成功标准（简化版）

### 7.1 功能标准
1. ✅ 项目可以编译通过
2. ✅ 数据源添加功能完整可用
3. ✅ 数据查询使用链式调用，无SQL字符串拼接
4. ✅ 数据清洗功能完整可用
5. ✅ 缓存功能正常工作
6. ✅ UI操作有基本反馈

### 7.2 代码质量标准
1. ✅ 编译零错误（警告可接受）
2. ✅ 关键功能有基本测试
3. ✅ 错误处理完善，不会崩溃
4. ✅ 代码结构清晰，易于理解

### 7.3 性能标准（基本要求）
1. ✅ 数据查询响应时间 < 5秒
2. ✅ 清洗处理不阻塞UI
3. ✅ 内存使用稳定

## 八、风险与应对

### 8.1 主要风险
1. **现有工具类不兼容**：QueryBuilder接口可能变化
2. **配置文件格式不统一**：数据库配置和缓存配置可能分散
3. **线程安全问题**：多线程访问共享资源

### 8.2 应对措施
1. **先验证再实施**：先测试现有工具类的可用性
2. **统一配置管理**：如有必要，统一配置文件格式
3. **简化线程模型**：避免复杂的线程交互

## 九、下一步行动建议

### 9.1 立即验证（今天）
1. **验证QueryBuilder可用性**：测试链式调用接口是否工作
2. **验证DataServiceCache可用性**：测试全局缓存单例是否可访问
3. **检查配置文件**：确认数据库和缓存配置文件的格式和位置

### 9.2 开始实施（明天开始）
1. **按第一阶段计划开始**：先修复编译错误
2. **分步骤验证**：每个组件完成后立即测试
3. **持续集成**：确保每次提交都可编译

### 9.3 文档更新
1. **更新现有文档**：反映新的设计方案
2. **创建API参考**：为新组件创建简单API文档
3. **记录技术决策**：说明为什么选择这个设计方案

---

**文档版本**：2.0  
**更新日期**：2026年2月24日  
**更新说明**：基于用户反馈重新设计，强调精简实用、使用现有工具类、链式查询、全局缓存和配置驱动  
**整合文档**：  
- 《项目状态总结与修改方向.md》  
- 《数据源添加与清洗全流程重新设计.md》  
- 《DataService_基于现有工具类的重构方案.md》  
- 用户的三点核心要求