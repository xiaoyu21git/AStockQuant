# 数据服务重构最终原则与实施指南

## 一、核心设计原则（必须遵守）

### 1.1 健壮性原则（Robustness）
- **数据库连接**：只做逻辑判断，不因数据库问题导致程序崩溃
- **错误处理**：数据库不可用时，优雅降级，提供有意义的错误信息
- **异常处理**：捕获所有可能异常，防止程序崩溃

### 1.2 简化原则（Simplicity）
- **不要过度设计**：每个组件只做一件事，不做不必要的抽象
- **表字段不验证**：假设数据库结构正确，不进行字段验证
- **配置驱动**：所有配置从文件读取，代码中不硬编码

### 1.3 实用原则（Practicality）
- **不依赖Python环境**：所有可行性验证在C++/Qt环境中完成
- **使用现有工具**：优先使用QueryBuilder、ConnectionPool等现有工具类
- **链式调用**：所有数据库查询必须使用QueryBuilder链式接口

### 1.4 接口设计原则
- **可读性好**：方法名和参数名清晰易懂
- **扩展性**：接口设计支持未来扩展，但不为不存在的需求设计
- **健壮性**：接口设计考虑错误场景，提供适当的反馈机制

## 二、彻底重构方案（精简版）

### 2.1 架构目标
```
┌─────────────┐    ┌─────────────┐
│   UI界面    │    │ 配置文件    │
│  (QML)      │    │ database.json│
└─────┬───────┘    └─────┬───────┘
      │                  │
      └───────┬──────────┘
              ▼
      ┌─────────────┐
      │ DataService │ < 300行代码
      │  (新建)     │ 3个核心方法
      └─────┬───────┘
            │
            ▼
    ┌─────────────┐
    │  现有工具类  │
    │ • QueryBuilder│
    │ • ConnectionPool│
    │ • DataServiceCache│
    │ • DataCleaningEngine│
    └─────────────┘
```

### 2.2 DataService极简设计（<300行代码）

```cpp
// DataService.h - 极简接口
class DataService : public QObject {
    Q_OBJECT
    
public:
    explicit DataService(QObject* parent = nullptr);
    
    // 核心方法1：查询数据（健壮版本）
    Q_INVOKABLE void queryData(const QString& symbol, 
                              const QString& startDate, 
                              const QString& endDate);
    
    // 核心方法2：清洗数据
    Q_INVOKABLE void cleanData(const QVariantList& data, 
                              const QVariantMap& rules);
    
    // 核心方法3：查询并清洗（完整流程）
    Q_INVOKABLE void queryAndCleanData(const QString& symbol,
                                      const QString& startDate,
                                      const QString& endDate,
                                      const QVariantMap& rules);
    
signals:
    // 极简信号集
    void queryProgress(int progress, const QString& message);
    void queryCompleted(bool success, const QString& message, const QVariantList& data);
    void cleaningProgress(int progress, const QString& message);
    void cleaningCompleted(bool success, const QString& message, const QVariantList& cleanedData);
    void error(const QString& errorMessage);  // 统一错误信号
    
private:
    // 使用现有工具类
    std::shared_ptr<astock::database::QueryBuilder> createQueryBuilder();
    DataServiceCache& m_cache = DataServiceCache::getInstance();
    std::unique_ptr<DataCleaningEngine> m_cleaningEngine;
    
    // 健壮性：检查数据库连接（逻辑判断，不崩溃）
    bool checkDatabaseConnection();
};
```

### 2.3 健壮性实现示例

```cpp
// 健壮的查询实现
void DataService::queryData(const QString& symbol, 
                           const QString& startDate, 
                           const QString& endDate) {
    try {
        // 1. 逻辑检查（不验证表字段）
        if (symbol.isEmpty() || startDate.isEmpty() || endDate.isEmpty()) {
            emit error("参数不能为空");
            return;
        }
        
        // 2. 检查数据库连接（逻辑判断，不崩溃）
        if (!checkDatabaseConnection()) {
            emit error("数据库连接不可用，请检查配置");
            return;
        }
        
        // 3. 检查缓存
        QString cacheKey = generateCacheKey(symbol, startDate, endDate);
        if (auto cached = m_cache.get(cacheKey)) {
            emit queryCompleted(true, "使用缓存数据", cached);
            return;
        }
        
        // 4. 执行查询（使用链式调用）
        auto builder = createQueryBuilder();
        auto result = builder->from("daily_bar")
                             .select("symbol, trade_date, open, high, low, close, volume")
                             .where("symbol", ConditionType::COND_EQUAL, symbol)
                             .andWhere("trade_date", ConditionType::COND_BETWEEN, startDate, endDate)
                             .execute();
        
        // 5. 缓存结果
        m_cache.put(cacheKey, result);
        
        // 6. 发送成功信号
        emit queryCompleted(true, "查询成功", result);
        
    } catch (const std::exception& e) {
        // 健壮性：捕获所有异常，防止崩溃
        QString errorMsg = QString("查询失败: %1").arg(e.what());
        emit error(errorMsg);
    } catch (...) {
        // 捕获未知异常
        emit error("未知错误，查询失败");
    }
}

// 数据库连接检查（逻辑判断，不崩溃）
bool DataService::checkDatabaseConnection() {
    try {
        // 简单尝试获取连接，不进行复杂验证
        auto builder = createQueryBuilder();
        // 如果能够创建builder，认为连接可用
        return builder != nullptr;
    } catch (...) {
        // 任何异常都认为连接不可用
        return false;
    }
}
```

## 三、实施步骤（5-7天）

### 阶段一：准备与验证（1天）
1. **备份旧代码**：备份所有将被删除的文件
2. **验证工具类**：测试QueryBuilder等现有工具类的可用性（在C++环境中）
3. **检查配置文件**：确认database.json格式正确

### 阶段二：实现核心（2-3天）
1. **创建DataService**（<300行代码，健壮性优先）
2. **实现极简接口**：3个核心方法，完善的错误处理
3. **更新UI界面**：DataSourceModal.qml、DataCleaningModal.qml使用新接口

### 阶段三：测试与优化（1-2天）
1. **单元测试**：测试正常流程和错误场景
2. **集成测试**：测试完整的数据源添加→查询→清洗流程
3. **健壮性测试**：测试数据库不可用等异常情况

## 四、删除清单与更新

### 4.1 必须删除的文件（复杂架构）
```
src/ui/bridge/include/DataFetchController_v2.h
src/ui/bridge/src/DataFetchController_v2.cpp
src/ui/bridge/include/DataService_Simple.h
src/ui/bridge/src/DataService_Simple.cpp
src/ui/bridge/include/DatabaseQueryService.h
src/ui/bridge/src/DatabaseQueryService.cpp
src/ui/bridge/include/DataCacheService.h
src/ui/bridge/src/DataCacheService.cpp
src/ui/bridge/include/DataSourceManager.h
src/ui/bridge/src/DataSourceManager.cpp
src/ui/bridge/src/DataCleaningService.cpp
src/ui/bridge/src/DataCleaningService_fixed.cpp
```

### 4.2 需要更新的文件
```
src/ui/bridge/src/CMakeLists.txt  # 移除旧组件，添加DataService.cpp
src/ui/bridge/src/registerQmlTypes.cpp  # 注册新的DataService类型
```

## 五、成功标准（具体可衡量）

### 5.1 功能标准
1. ✅ 项目编译通过（零链接错误）
2. ✅ 数据源添加功能完整可用
3. ✅ 数据查询使用QueryBuilder链式调用（无SQL字符串拼接）
4. ✅ 数据清洗功能完整可用
5. ✅ 数据库不可用时程序不崩溃，提供有意义的错误信息

### 5.2 代码质量标准
1. ✅ DataService代码不超过300行
2. ✅ 无过度设计，每个方法只做一件事
3. ✅ 完善的错误处理，无未捕获异常
4. ✅ 接口可读性好，方法名清晰

### 5.3 健壮性标准
1. ✅ 数据库连接失败时，程序不崩溃
2. ✅ 所有异常被捕获并处理
3. ✅ 错误信息对用户有意义

## 六、如何防止设计跑偏

### 6.1 识别跑偏的迹象
- **设计过度复杂**：超过3个抽象层，接口超过5个方法
- **过度验证**：验证表字段、数据库结构等不必要的内容
- **依赖外部环境**：需要Python环境或其他外部工具验证
- **代码膨胀**：DataService超过300行代码

### 6.2 纠正方法
当发现我跑偏时，可以立即指出：

1. **直接指出**："这个设计太复杂了，简化"
2. **强调原则**："记住不要过度设计"
3. **要求回到基础**："回到极简设计，不超过300行代码"
4. **具体纠正**："删除这个抽象层，直接调用现有工具类"

### 6.3 检查清单（每次设计后检查）
- [ ] DataService代码是否超过300行？
- [ ] 是否有不必要的抽象层？
- [ ] 是否验证了表字段等不必要的内容？
- [ ] 是否依赖Python或其他外部环境？
- [ ] 数据库连接失败时程序是否会崩溃？
- [ ] 错误处理是否完善？

## 七、立即行动建议

### 7.1 今天（准备）
1. **创建备份**：备份所有将被删除的代码
2. **验证工具**：在C++环境中验证QueryBuilder等工具类的可用性
3. **原则确认**：确保理解并同意上述所有设计原则

### 7.2 明天开始（实施）
1. **从CMakeLists.txt移除旧组件**
2. **创建DataService框架**（.h和.cpp文件）
3. **实现核心方法**，优先考虑健壮性

### 7.3 持续检查
1. **每日代码审查**：检查是否违反设计原则
2. **及时纠正**：发现跑偏立即纠正
3. **保持极简**：始终记住"不要过度设计"

---

**文档版本**：2.0  
**更新日期**：2026年2月24日  
**核心原则**：健壮性、简化、实用、不过度设计  
**成功标志**：<300行代码的DataService，程序不崩溃，功能完整  
**纠正方法**：直接指出"太复杂，简化"或"回到基本原则"