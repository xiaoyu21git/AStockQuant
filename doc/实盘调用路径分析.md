# 实盘完整调用路径：从启动 → 行情 → 信号 → 下单

> 基于实际代码的真实调用路径分析，每步标注了源文件和行号。
> 生成日期：2026-06-25

---

## 一、应用启动阶段

```
main()                                              [src/app/main.cpp:13]
  │
  ├─ QApplication app(argc, argv)
  ├─ AppBootstrap bootstrap
  │
  ├─ bootstrap.init()                               [src/app/src/AppBootstrap.cpp:214]
  │   ├─ initConfiguration()                        [line 292]
  │   │   ├─ runtimeDirectories()                   → config/logs/files/cache/temp
  │   │   ├─ Foundation::instance().initialize()    → 日志/配置基础库
  │   │   └─ ConfigManager::instance().initialize() → profile="development"
  │   │
  │   ├─ initServices()                             [line 345]
  │   │   ├─ EventBus::create() + start()           → 全局事件总线
  │   │   └─ register_engine_event_bus(m_eventBus)
  │   │
  │   └─ initDatabase()                             [line 383]
  │       └─ DatabaseConnectionManager::instance()  → NativeMySQL连接池
  │
  ├─ bootstrap.start()                              [line 246]
  │   ├─ initQmlEngine()                            [line 397]
  │   │   ├─ QQmlApplicationEngine()
  │   │   └─ VasAurora(engine)                      [src/app/src/VasAurora.cpp:8]
  │   │       ├─ registerQmlTypes()                 → 注册所有QML桥接类型
  │   │       └─ engine->load("main.qml")            → 加载UI
  │   │
  │   └─ scheduleDeferredStartupInitialization()    [line 415]
  │       └─ QTimer(0) → initializeDeferredUiServices()
  │           └─ QTimer(0) → initializeDeferredDomainServices()
  │               └─ initializeDeferredTradingServices()
  │                   └─ scheduleOptionalConnectorReconcile()
  │                       └─ reconcileOptionalConnectors()  [line 503]
  │                           ├─ JujinMarketConnector::start()    ← ═══ 行情入口
  │                           └─ TradingSystem::initializeWithBroker("jujin","live")
  │
  └─ app.exec()  → Qt事件循环
```

---

## 二、掘金行情连接阶段（JujinMarketConnector）

```
JujinMarketConnector::start()                      [src/app/src/JujinMarketConnector.cpp:215]
  │
  ├─ 读取 config/trading_connection.json → token, accountId, gmStrategyId
  │
  ├─ JujinApi::initialize(config)                  → 初始化掘金SDK
  ├─ JujinApi::connect()                           → 连接掘金服务器
  ├─ register_shared_jujin_api(m_api)              → 注册全局共享API
  │
  ├─ 启动订阅线程 → processSubscriptionRequests()  [line 475]
  │   ├─ 查询 ref.symbol_info 表 → 获取全市场股票代码
  │   └─ 循环: enqueueWatchSymbol() → subscribeSymbolBatch()
  │       └─ m_api->subscribe_market_data(symbols, TICK)
  │       └─ m_api->subscribe_market_data(symbols, BAR_1M)
  │
  ├─ 订阅 EventBus 事件 "trading.market.tick"      [line 336] ← ═══ 核心回调
  │   └─ 回调: TradingSystem::pushMarketData(sym, price, vol, date)
  │
  └─ 订阅 EventBus 事件 "trading.market.bar"       [line 362]
      └─ 回调: TradingSystem::pushMarketData(sym, price, vol, date)
```

---

## 三、策略启动阶段（QML → Bridge → Engine）

```
[QML] StrategyLibraryPage.qml                      用户点击"启动"按钮
  │
  └─ StrategyBridge::start(strategyId)              [src/ui/bridge/src/StrategyBridge.cpp:600]
      │
      ├─ m_repo->updateStatus(id, Active)            → 写DB状态
      │
      └─ m_startupPool->post([this, id] {            → 线程池异步执行
          │                                           [line 625]
          ├─ StrategyManager::createEngine(id, factorSvc)  [src/domain/strategy/src/StrategyManager.cpp:13]
          │   │
          │   └─ StrategyEngine::fromDb(id, factorSvc)     [src/domain/strategy/src/StrategyEngineFacade.cpp:64]
          │       │
          │       ├─ 查询MySQL: SELECT metadata_json, parameters
          │       │   FROM strategy WHERE strategy_id = ?
          │       │
          │       ├─ 解析 JSON → StrategyCreationParams (topN, periods, weights...)
          │       │
          │       ├─ Builder模式构建引擎:
          │       │   StrategyEngine::builder()
          │       │     .withFactorService(factorSvc)        → RuntimeFactorSvc
          │       │     .maxStrategies(maxPositions)
          │       │     .build()
          │       │     │
          │       │     └─ 内部创建:
          │       │         ├─ NoOpFactorService (非因子策略) 或 注入的RuntimeFactorSvc
          │       │         ├─ LocalRuleEvaluationService      → 规则检查(score>=0, weight<=1)
          │       │         └─ StrategyService(factorSvc, ruleEvalSvc)
          │       │
          │       ├─ 因子策略:
          │       │   ├─ RuntimeFactorSvc::setFactorIds(factorIds)
          │       │   ├─ 创建 MultiFactorSelectionStrategy(strategyDef)
          │       │   └─ createMultiFactorSelectionRuntimeStrategy() → 注册engine
          │       │       见 RuntimeStrategyFactory.cpp:76
          │       │
          │       └─ 非因子策略:
          │           ├─ 根据 behaviorKind 选择引擎:
          │           │   TrendFollowing | MeanReversion | Momentum |
          │           │   Arbitrage | EventDriven | HighFrequency | Custom
          │           └─ 注册到engine (见 StrategyEngineFacade.cpp:219-244)
          │
          ├─ engine->start()                                → StrategyService state=Running
          │
          ├─ 查询PostgreSQL历史行情:                         [line 673]
          │   │  SELECT symbol, trade_date, open, high, low, close, volume
          │   │  FROM mkt.daily_bar ... WHERE trade_date >= 回溯窗口
          │   │
          │   ├─ 非因子策略: CachedMarketDataView::fromJson(json)
          │   └─ 因子策略:   MarketDataRepository::queryAllMarketDailyBarWithFields()
          │                  → RuntimeFactorSvc::buildLiveView()
          │
          ├─ engine->setLiveMarketView(view)                 [line 756]
          │   ├─ 因子策略: RuntimeFactorSvc::setLiveMarketView(view)
          │   └─ 非因子策略: setContextHistoricalView(view)  → 注入context.historicalViewPtr()
          │
          ├─ mgr.setOrderListener(StrategyOrderForwarder)    [line 776]
          │   └─ 所有引擎共享同一个订单转发器
          │
          ├─ engine->startLiveLoop()                         [line 778]
          │   └─ 创建专用线程: ThreadPoolExecutor(1,1,"StrategyEngineLiveLoop")
          │       └─ drainQueue() → 阻塞等待行情入队          [line 480]
          │
          └─ QMetaObject::invokeMethod → emit started(id)    → QML UI更新
      });
```

---

## 四、实时行情推送路径（最关键的数据流）

```
掘金SDK GmStrategySession::on_tick(gm_tick)        [src/ui/bridge/src/GmStrategySession.cpp:1270]
  │
  ├─ 构建 EventFormat { "trading.market.tick",
  │     symbol, price=gm_tick->price,
  │     volume=gm_tick->cum_volume,
  │     tradingDay=YYYYMMDD }
  │
  └─ eventBus->publish(event, HIGH)  ─────────────────┐
                                                       │
        ┌──────────────────────────────────────────────┘
        ▼
JujinMarketConnector 订阅回调                          [JujinMarketConnector.cpp:336]
  │
  └─ TradingSystem::pushMarketData(symbol, price, volume, tradingDay)
                                                        [src/app/system/TradingSystem.cpp:120]
      │
      ├─ m_latestPrices[symbol] = price                 ← 缓存最新价
      │
      └─ MarketDataAdapter adapter
          adapter.pushTick(symbol, price, volume, tradingDay)
                                                        [src/domain/strategy/include/MarketDataAdapter.h:25]
          │
          ├─ resolveInstrumentId(symbol)                 → "000001.SZ" → InstrumentId(1)
          ├─ MarketDataPoint mdp(instId, price, volume, tradingDay)
          │
          └─ StrategyManager::pushMarketData(mdp)       [src/domain/strategy/src/StrategyManager.cpp:94]
              │
              └─ for each engine: engine->enqueueMarketData(mdp)
                                                        [src/domain/strategy/src/StrategyEngineFacade.cpp:422]
                  │
                  ├─ lock(m_queueMutex)
                  ├─ m_mdpQueue.push(mdp)                → 入队（上限5000）
                  └─ m_queueCv.notify_one()              → 唤醒drainQueue线程
```

---

## 五、信号生成路径（drainQueue → 因子计算 → 策略评估）

```
StrategyEngine::drainQueue()                          [src/domain/strategy/src/StrategyEngineFacade.cpp:480]
  │  (运行在专用线程 "StrategyEngineLiveLoop")
  │
  ├─ m_queueCv.wait() → 阻塞等待行情
  ├─ mdp = m_mdpQueue.front(); m_mdpQueue.pop()
  │
  └─ step(mdp)                                        [line 497]
      │
      └─ strategyService_->onMarketDataPoint(mdp)     [src/domain/strategy/src/StrategyServer.cpp:207]
          │
          ├─ 检查 state == Running
          │
          ├─ factorService_.updateIncremental(mdp)     → RuntimeFactorSvc 累积最新tradeDay+symbol
          │
          └─ evaluateAndCheckRulesLowLatency()         [line 379]
              │
              │  ┌─── 对每个注册的策略 ───┐
              │  │                         │
              │  ├─ evaluateEntrySignals() │          [line 455]
              │  │   │                     │
              │  │   ├─ factorService_.copySnapshots(snapshots)
              │  │   │   │                             [RuntimeFactorSvc.cpp:278]
              │  │   │   │
              │  │   │   └─ for each factorId:
              │  │   │       getValues(instanceId, tradeDay, symbols)
              │  │   │         │                         [RuntimeFactorSvc.cpp:150]
              │  │   │         │
              │  │   │         └─ FactorEngine::computeSingleDate(
              │  │   │               instanceId, dateBuf, symbols, m_liveMarketView)
              │  │   │             → 返回 { symbol → factorValue }
              │  │   │
              │  │   └─ entry.strategy->evaluate(factorSnapshots, context, signalBuffer_)
              │  │       │
              │  │       ├──【因子策略路径】MultiFactorSelectionRuntimeStrategy::evaluate()
              │  │       │    [RuntimeStrategyFactory.cpp:92]
              │  │       │   │
              │  │       │   ├─ computeCompositeScores(factorSnapshots)
              │  │       │   │   [MultiFactorSelectionStrategy.cxx:34]
              │  │       │   │   │
              │  │       │   │   ├─ buildCompositeScores():
              │  │       │   │   │   ├─ 按factorId分组统计 mean/std
              │  │       │   │   │   ├─ Z-score标准化: (value - mean) / std
              │  │       │   │   │   ├─ (可选)行业中性化
              │  │       │   │   │   ├─ 加权求和: Σ(weight × zScore)
              │  │       │   │   │   └─ 只保留全部因子都有的标的
              │  │       │   │   │
              │  │       │   │   └─ Top-N选择 (nth_element 部分排序)
              │  │       │   │
              │  │       │   ├─ 买/卖分离: score>0→Buy, score<0→Sell(允许做空时)
              │  │       │   ├─ 权重计算: EQUAL(均分) 或 SIGNAL_STRENGTH(按|score|)
              │  │       │   └─ 输出 StrategySignal{instrumentId, side, score, weight}
              │  │       │
              │  │       └──【非因子策略路径】NonFactorStrategy::evaluate()
              │  │            [NonFactorStrategy.h:40]
              │  │           │
              │  │           ├─ 从 context.historicalViewPtr() 读取OHLCV矩阵
              │  │           ├─ 对每个标的 evaluateSymbol(closePrices):
              │  │           │   ├─ TrendFollowing:   TA_SMA快慢线交叉
              │  │           │   ├─ MeanReversion:    TA_RSI超买超卖(30/70)
              │  │           │   ├─ Momentum:         TA_MACD金叉死叉
              │  │           │   ├─ Arbitrage:        TA_BBANDS布林带%B(<0.1买,>0.9卖)
              │  │           │   ├─ EventDriven:      量价异动(5日vs20日均价+波动率)
              │  │           │   ├─ HighFrequency:    短期动量(ROC+方向一致性)
              │  │           │   └─ Custom:           多指标投票(MA+RSI+MACD≥2同向)
              │  │           └─ 排序限流: 卖单优先, 买单Top-N
              │  │
              │  ├─ for each signal:
              │  │   │
              │  │   ├─ ruleEvaluationService_.evaluate(signal, ruleSetId, context)
              │  │   │   └─ LocalRuleEvaluationService:
              │  │   │       ├─ 规则1: score >= 0.0
              │  │   │       └─ 规则2: |targetWeight| <= 1.0
              │  │   │
              │  │   └─ handleRuleEvaluationResult()  [line 478]
              │  │       │
              │  │       ├─ 通过: buildOrderRequest() → DefaultOrderBuilder
              │  │       │   └─ OrderRequest(strategyInstanceId, instrumentId, side, quantity)
              │  │       │   → push到 pendingOrderBuffer_
              │  │       └─ 拒绝: diagnostics + 统计
              │  │
              │  └─ flushPendingOrders()              [line 550]
              │      └─ orderSink_ 为空（实盘不设）→ 订单留在buffer中
              │
              └─ 返回 flowResult

      │
      ├─ collectOrders(flowResult)                    [StrategyEngineFacade.cpp:404]
      │   └─ strategyService_->copyPendingOrders(outputOrders)
      │
      └─ 如有订单: m_orderListener->onOrders(*orders) [line 498]
```

---

## 六、订单转发与执行路径

```
StrategyOrderForwarder::onOrders(orders)             [StrategyBridge.cpp:150]
  │
  └─ for each OrderRequest:
      │
      ├─ 创建 TradeOrder:
      │   ├─ order.setSymbol(resolver(instrumentId))  → "000001.SZ"
      │   ├─ order.setSide(Buy/Sell)
      │   ├─ order.setQuantity(quantity)
      │   ├─ order.setStrategyId(strategyId)
      │   └─ order.setPrice(1.0)                      → 实盘由网关确定
      │
      └─ TradingSystem::submitOrder(order)            [TradingSystem.cpp:215]
          │
          ├─ buildRiskInput(order)                    [line 137]
          │   └─ 填充 RiskInput:
          │       ├─ strategyId, symbol, price, quantity
          │       ├─ accountSnapshot().totalAsset / marketValue
          │       ├─ positions() → 标的持仓快照
          │       ├─ 风控配置: stopLoss%, takeProfit%
          │       └─ drawdown, signalStrength
          │
          └─ m_tradeEngine->submitOrder(order, riskInput)
                                                        [TradeExecutionEngine.cpp:182]
              │
              │  ╔═══════════════════════════════════════╗
              │  ║   4级订单流水线                        ║
              │  ╚═══════════════════════════════════════╝
              │
              ├─ Stage 1: validateOrder(order)
              │   ├─ symbol非空
              │   ├─ price > 0
              │   ├─ quantity > 0
              │   └─ board lot (整手100)
              │
              ├─ Stage 2: 调度冲突检查
              │   ├─ checkExecutionPause       → 异常拒单后暂停?
              │   ├─ checkManualCheckpoint     → 需人工审批?
              │   ├─ checkPartialFillAdvance   → 前置批次未成交?
              │   └─ checkPendingOrderConflict → 同标的反向挂单?
              │
              ├─ Stage 3: RiskEvaluator::evaluateOrder(riskInput)
              │   └─ 风控规则:
              │       ├─ 回撤限制
              │       ├─ 敞口限制
              │       ├─ 止损止盈
              │       └─ 持仓上限
              │
              ├─ Stage 4: 网关提交
              │   ├─ OrderRequest::create(strategyId, symbol, side, type, price, qty)
              │   ├─ 标记状态 Submitted → appendRecentOrder()
              │   ├─ 回调: m_orderAcceptedCallback → TradeExecutionBridge → QML信号
              │   │
              │   └─ m_gateway->submitOrder(request, asyncCallback)
              │       │
              │       └─ JujinBrokerGateway::submitOrder()
              │           └─ 掘金SDK: order_volume() / order_value()
              │               │
              │               └─ asyncCallback(OrderStatus):
              │                   ├─ 更新 brokerOrderId, status, filledPrice/Qty
              │                   └─ m_orderUpdateCallback → QML orderStatusChanged
              │
              └─ 返回 SubmitResult
```

---

## 七、完整数据流总结图

```
 ┌─────────────────────────────────────────────────────────────────────────┐
 │                         掘金SDK (gmsdk)                                  │
 │  GmStrategySession::on_tick(tick)                                       │
 │  → 发布 EventBus "trading.market.tick" {symbol, price, volume, date}   │
 └──────────────────────────┬──────────────────────────────────────────────┘
                            │ EventBus
                            ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │  JujinMarketConnector                                                     │
 │  订阅 "trading.market.tick" → TradingSystem.pushMarketData()             │
 └──────────────────────────┬───────────────────────────────────────────────┘
                            │
                            ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │  MarketDataAdapter                                                        │
 │  symbol → InstrumentId → MarketDataPoint                                 │
 └──────────────────────────┬───────────────────────────────────────────────┘
                            │
                            ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │  StrategyManager::pushMarketData(mdp)                                     │
 │  → 遍历所有引擎: engine->enqueueMarketData(mdp)                          │
 │  → 写入 m_mdpQueue → notify drainQueue线程                               │
 └──────────────────────────┬───────────────────────────────────────────────┘
                            │ drainQueue线程被唤醒
                            ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │  StrategyService::onMarketDataPoint(mdp)  (持有mutex锁)                   │
 │                                                                           │
 │  ① RuntimeFactorSvc::updateIncremental(mdp)  → 记录最新 tradeDay+symbols │
 │                                                                           │
 │  ② evaluateAndCheckRulesLowLatency():                                     │
 │     ├─ RuntimeFactorSvc::copySnapshots() → FactorEngine::computeSingle() │
 │     ├─ strategy->evaluate(factorSnapshots, context) → 信号列表            │
 │     │   ├─ 因子策略: Z-score标准化 → 加权求和 → Top-N → buy/sell        │
 │     │   └─ 非因子策略: OHLCV → TA-Lib(SMA/RSI/MACD/BBands) → buy/sell   │
 │     ├─ LocalRuleEvaluationService::evaluate(signal) → score>=0, |w|<=1  │
 │     └─ handleRuleEvaluationResult() → DefaultOrderBuilder → OrderRequest │
 │                                                                           │
 │  ③ flushPendingOrders() → orderSink_为空，订单留在buffer                  │
 └──────────────────────────┬───────────────────────────────────────────────┘
                            │ collectOrders() 返回 vector<OrderRequest>
                            ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │  drainQueue(): m_orderListener->onOrders(orders)                         │
 │  └─ StrategyOrderForwarder::onOrders()                                    │
 │     └─ OrderRequest → TradeOrder → TradingSystem.submitOrder()           │
 └──────────────────────────┬───────────────────────────────────────────────┘
                            │
                            ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │  TradeExecutionEngine::submitOrder(order, riskContext)                    │
 │  ① validateOrder() → symbol/price/qty/board lot                         │
 │  ② 调度冲突检查 (暂停/审批/前序未成交/反向挂单)                            │
 │  ③ RiskEvaluator::evaluateOrder() → 风控审批                             │
 │  ④ JujinBrokerGateway::submitOrder() → 掘金SDK → 交易所                  │
 └──────────────────────────────────────────────────────────────────────────┘
```

---

## 八、关键代码位置索引

| 阶段 | 文件 | 关键行/方法 |
|------|------|-----------|
| 入口 | `src/app/main.cpp` | `main()` L13 |
| 启动初始化 | `src/app/src/AppBootstrap.cpp` | `init()` L214, `start()` L246 |
| UI加载 | `src/app/src/VasAurora.cpp` | `VasAurora()` L8-14 |
| 掘金连接 | `src/app/src/JujinMarketConnector.cpp` | `start()` L215, tick订阅 L336 |
| 策略启动 | `src/ui/bridge/src/StrategyBridge.cpp` | `start()` L600 |
| 引擎构建 | `src/domain/strategy/src/StrategyEngineFacade.cpp` | `fromDb()` L64 |
| 行情格式化 | `src/domain/strategy/include/MarketDataAdapter.h` | `pushTick()` L25 |
| 行情分发到引擎 | `src/domain/strategy/src/StrategyManager.cpp` | `pushMarketData()` L94 |
| 行情入队 | `src/domain/strategy/src/StrategyEngineFacade.cpp` | `enqueueMarketData()` L422 |
| 后台循环 | `src/domain/strategy/src/StrategyEngineFacade.cpp` | `drainQueue()` L480 |
| 信号生成主逻辑 | `src/domain/strategy/src/StrategyServer.cpp` | `onMarketDataPoint()` L207 |
| 低延迟评估 | `src/domain/strategy/src/StrategyServer.cpp` | `evaluateAndCheckRulesLowLatency()` L379 |
| 策略信号评估 | `src/domain/strategy/src/StrategyServer.cpp` | `evaluateEntrySignals()` L455 |
| 因子快照 | `src/domain/strategy/src/RuntimeFactorSvc.cpp` | `copySnapshots()` L278 |
| 因子值查询 | `src/domain/strategy/src/RuntimeFactorSvc.cpp` | `getValues()` L150 |
| 因子策略评估 | `src/domain/strategy/src/RuntimeStrategyFactory.cpp` | MultiFactorSelectionRuntimeStrategy::`evaluate()` L92 |
| 因子复合评分 | `src/domain/strategies/src/MultiFactorSelectionStrategy.cxx` | `computeCompositeScores()` L34 |
| 非因子策略基类 | `src/domain/strategy/include/NonFactorStrategy.h` | `evaluate()` L40 |
| 趋势跟踪(MA) | `src/domain/strategy/include/NonFactorStrategy.h` | `TrendFollowingStrategy` L124 |
| 均值回归(RSI) | `src/domain/strategy/include/NonFactorStrategy.h` | `MeanReversionStrategy` L159 |
| 动量(MACD) | `src/domain/strategy/include/NonFactorStrategy.h` | `MomentumStrategy` L191 |
| 套利(BBands) | `src/domain/strategy/include/NonFactorStrategy.h` | `ArbitrageStrategy` L227 |
| 事件驱动 | `src/domain/strategy/include/NonFactorStrategy.h` | `EventDrivenStrategy` L271 |
| 高频 | `src/domain/strategy/include/NonFactorStrategy.h` | `HighFrequencyStrategy` L331 |
| 自定义多指标 | `src/domain/strategy/include/NonFactorStrategy.h` | `CustomStrategy` L367 |
| 规则检查 | `src/domain/strategy/src/StrategyPseudoServices.cpp` | `LocalRuleEvaluationService::evaluate()` L23 |
| 订单构建 | `src/domain/strategy/src/StrategyPseudoServices.cpp` | `DefaultOrderBuilder::buildOrder()` L235 |
| 订单转发器 | `src/ui/bridge/src/StrategyBridge.cpp` | `StrategyOrderForwarder::onOrders()` L150 |
| 风控输入构建 | `src/app/system/TradingSystem.cpp` | `buildRiskInput()` L137 |
| 订单提交 | `src/app/system/TradingSystem.cpp` | `submitOrder()` L215 |
| 4级订单流水线 | `src/domain/trading/TradeExecutionEngine.cpp` | `submitOrder()` L182 |
| 订单验证 | `src/domain/trading/TradeExecutionEngine.cpp` | `validateOrder()` L62 |
| 调度冲突检查 | `src/domain/trading/TradeExecutionEngine.cpp` | L96-L177 |
| 风控评估 | `src/domain/strategy/include/RiskEvaluator.h` | `evaluateOrder()` |
| 网关提交 | `src/domain/trading/TradeExecutionEngine.cpp` | L220-L272 |
| 掘金SDK行情回调 | `src/ui/bridge/src/GmStrategySession.cpp` | `on_tick()` L1270 |
| 交易桥接层 | `src/ui/bridge/src/TradingBridges.cpp` | `TradeExecutionBridge::submitOrder()` L144 |
| 持仓账户桥接 | `src/ui/bridge/src/TradingBridges.cpp` | `PositionAccountBridge` L316 |
| 风控桥接 | `src/ui/bridge/src/TradingBridges.cpp` | `RiskControlBridge` L409 |

---

## 九、线程模型

| 线程 | 作用 | 源文件 |
|------|------|--------|
| **主线程 (Qt Event Loop)** | QML UI渲染 + 桥接层信号处理 | `main.cpp` |
| **JMC订阅线程** | 处理掘金行情订阅请求 | `JujinMarketConnector.cpp` L300 |
| **JMC初始订单同步线程** | 启动时同步已有订单 | `JujinMarketConnector.cpp` L695 |
| **StrategyEngineLiveLoop** (每个策略1个) | drainQueue() 阻塞等待行情→信号→订单 | `StrategyEngineFacade.cpp` L480 |
| **StrategyBridgeStartup** | 策略启动异步工作线程 | `StrategyBridge.cpp` L622 |
