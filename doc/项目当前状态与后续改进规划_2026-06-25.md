# 项目当前状态与后续改进规划

> **ℹ️**: 文中 MySQL 迁移相关内容已于 2026-07 完成。MySQL 已退役，PostgreSQL 为当前主库。

**日期**：2026-07-02  
**当前版本**：v0.11.0 (`0e314b5`)，**1.0.0 四条主链全部达标**

### v0.11.0 新增（2026-07-01 ~ 07-02）

| 提交 | 模块 | 内容 |
|------|------|------|
| `af74f14` | 行情桥接 | 统一 MarketDataBridge，EventBus tick 驱动，零定时器 |
| `5548ddf` | 风控 | 全局巡检止损/止盈订单提交 TradeEngine |
| `84ff87b` | 策略调度 | DailyEodScheduler，14:50 收盘前下单 + 补单 |
| `0e45b0d` | 持久化 | live_order 订单流水 + OrderRecorder 异步写入 |
| `e6e745e` | 持久化 | 订单+成交合并一张 live_order 表 |
| `c07a5c3` | 持久化 | live_account_daily 每日账户快照 |
| `fc93c20` | 文档 | 全量文档/内存标注 PG 迁移完成 |
| `0e314b5` | 文档 | 5 轮代码验证 + 归档 0.10.x/1.0.0 清单 |

---

## 一、数据库迁移完成度

### ✅ 已完成的迁移

| 系统 | 改动 | 状态 |
|---|---|---|
| PG 环境 | PostgreSQL 18，7 个 schema，46 张表，2 视图 | ✅ |
| 数据迁移 | 2100 万行（daily_bar 1103 万 + financial 431 万 + 其余） | ✅ |
| C++ Native 路径 | `NativePgDatabase`（libpq），`NativeMySQLConnectionPool` 改 PG | ✅ |
| C++ 策略/因子 | `StrategyRepository` 重写，`FactorService` 改 `ISqlDatabase` | ✅ |
| C++ 行情 | `MarketDataRepository` 全部 PG，`search_path` 全局 schema | ✅ |
| Qt DB 移除 | `ConnectionPool`、`QtMySQLDatabase`、`FactorRepository`、`QueryBuilder` 等 15 文件删除 | ✅ |
| Python 日更核心 | `daily_pipeline.py`、`run_daily_update_pipeline.py` 切 psycopg2，symbol→symbol_id | ✅ |
| Python 交易日历 | `data_source_config.py` PG 源 | ✅ |
| MySQL 退役 | `DROP DATABASE astock_quant` | ✅ |

### ⚠️ 部分完成

| 项目 | 现状 | 待处理 |
|---|---|---|
| Python 辅助脚本 | 核心日更脚本已切 PG | 30+ 辅助脚本仍引用 pymysql |
| CAST AS CHAR | `FactorInstanceManager`、`DataAvailabilityChecker` 已改 `::text` | 其他文件可能有残留 |
| 磁盘空间 | D 盘 78%（PG 已迁 E 盘，释放 23G） | — |

### ❌ 未完成

- `sync_trade_calendar.py`（pymysql → psycopg2）
- Python 辅助脚本批量迁移
- 性能优化（`resolve_backfill_range` JOIN 查询可合并为单次）
- 数据对账（MySQL 已删，无法直接对比）

---

## 二、当前项目缺口

### 2.1 功能缺位（vs. 设计文档的 1.0.0 清单）— 2026-07-02 更新

| 闭环 | 1.0.0 要求 | 现状 |
|---|---|---|
| 数据管理 | 查询/预览/清洗稳定 | ✅ PG 全通，`DataCleaningPersistence` 已删除 |
| 因子分析 | 工作台可创建/回测/展示 | ✅ FactorEngine+Orchestrator 完整链路 |
| 策略回测 | 完整可跑/结果可回写 | ✅ PG 下编译通过，StrategyBacktestBridge 完整 |
| 清洗质量 | 规则/任务/结果稳定 | ✅ DataCache Arrow 文件路径，不依赖 DB |
| 主库迁移 | PG 冻结/MySQL 不再写入 | ✅ C++ 主力路径全切 PG，MySQL 数据目录已删除 |
| **实盘行情链** | 不在 1.0.0 清单 | ✅ MarketDataBridge 统一桥接 + EventBus tick |
| **实盘交易链** | 不在 1.0.0 清单 | ✅ TradeEngine → gmsdk place_order, 风控+止损 |
| **实盘持久化** | 不在 1.0.0 清单 | ✅ live_order + live_account_daily |
| **日频调度** | 不在 1.0.0 清单 | ✅ DailyEodScheduler 14:50 下单 + 补单 |

> 四条主链 + 四条实盘链全部达标。剩余工作为 P1 优化项。

### 2.2 技术债务

| 问题 | 严重程度 | 说明 |
|---|---|---|
| Python 脚本 `pymysql` 残留 | 🟡 | 30+ 文件，非核心但功能受损 |
| 数据对账未做 | 🟡 | MySQL 已删，无法直接对账 |
| `resolve_backfill_range` 性能 | 🟡 | 交互模式下仍跑慢查询 |
| 多因子策略信号验证 | 🟡 | 之前 129→0 已修复但未确认 |

### 2.3 设计文档缺口

| 设计文档 | 质量 | 缺口 |
|---|---|---|
| `PostgreSQL数据库设计规范_终版.md` | ✅ 封版 | `index_constituents` 字段与 MySQL 实际结构不符 |
| `MySQL到PostgreSQL迁移计划.md` | ✅ | 需补充 C++ 代码已完成的标注 |

---

## 三、后续改进优先级

### P1 — 稳定项（2 周内）

4. **Python 辅助脚本迁移** — `sync_trade_calendar.py` 等 30+ 脚本 pymysql→psycopg2
5. **C++ `DatabaseProvider.cpp`** — `#include <mysql.h>` → libpq
6. **`resolve_backfill_range` 性能优化** — 合并为单次查询
7. **C++ 全量回归测试** — 回测、因子、策略各跑一轮

### P2 — 完善项（1 个月内）

8. **C++ 掘金日线数据更新** — `history_bars` 替代 Python 日更脚本
9. **TimescaleDB 压缩策略调优** — 验证 90 天压缩是否生效
10. **Python 脚本统一 `db_config.py`** — 消除硬编码连接

---

## 四、版本定位与路线图

### 4.1 当前版本线

| 版本线 | 状态 | 说明 |
|---|---|---|
| **0.9.x 稳定版** | 历史基线 | v0.9.0 tag `3033c97` |
| **0.10.x 推进版** | ✅ 已完成 | 所有目标达标，PG 迁移完成 |
| **v0.11.0** | ✅ 当前 `dev` | 实盘持久化+交易链+日频调度+统一行情桥接 |
| **1.0.0** | ⬜ 目标 | 清完 P1 残留后打 tag |

### 4.2 1.0.0 达标判定 (2026-07-02)

| 闭环 | 要求 | 进度 | 达标? |
|---|---|---|---|
| 数据管理 | 查询/预览/清洗稳定 | 100% | ✅ |
| 因子分析 | 工作台可创建/回测/展示 | 100% | ✅ |
| 策略回测 | 完整可跑/结果可回写 | 100% | ✅ |
| 清洗质量 | 规则/任务/结果稳定 | 100% | ✅ |
| 主库迁移 | PG 冻结/MySQL 退役 | 90% | ⚠️ Python 残留 + DatabaseProvider.cpp |
| 实盘交易 | 下单/风控/持久化 | 100% | ✅ |
| 主库迁移 | PG 冻结/MySQL 不写入 | 95% | ⚠️ 数据对账未做 |
| 主回归 | 长期稳定 | 0% | ❌ 未建立回归测试基线 |

**加权进度**：约 72%。保守估计 1.0.0 还需 **3-4 周**。

### 4.5 推荐发布节奏

```
现在 ──→ P0 阻塞修复 (1周) ──→ 0.9.0 稳定版
  │                                    │
  │                                    ├── bug fix only
  │                                    └── 可演示、可回归
  │
  └──→ P1 稳定项 (2周) ──→ P2 完善 (2周) ──→ 1.0.0-rc1
```

**0.9.0 冻结标准**：
- D 盘扩容 + PG 迁 E 盘 ✅
- `DataCleaningPersistence` 删除（死代码）✅
- Python 日更脚本 `--baostock-only` 可执行 ⬜
- `dev` 拉出 `release/0.9.0`，此后不在稳定版加新功能

**1.0.0 发布标准**：
- 清单 6 项全部达标
- 回测回归测试通过
- PG 数据对账完成
- 实盘链路端到端验证通过
