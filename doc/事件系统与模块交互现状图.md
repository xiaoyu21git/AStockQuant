# 事件系统与模块交互现状图

这张图描述的是当前仓库代码里已经确认存在的事件系统接线关系，反映现状，不是理想目标架构。

```mermaid
flowchart LR
    subgraph Ext[外部/接入层]
        GM[掘金/第三方行情交易接口]
    end

    subgraph TP[第三方适配层]
        JAPI[JujinApi]
        JEB[JujinApi_EventBus]
    end

    subgraph ENG[引擎层]
        EI[EngineImpl]
        EBUS[EventBus]
        REG[GlobalEventBusRegistry]
        TRG[TriggerBusBridge]
    end

    subgraph DOM[领域/业务层]
        STRAT[策略模块]
        RISK[风控模块]
        EXEC[下单执行模块]
        POS[持仓/账户模块]
        BT[回测模块]
    end

    subgraph APP[应用/UI层]
        BOOT[AppBootstrap]
        SS[StrategyService]
        RS[RiskConfigService / RiskMonitorService]
        UI[QML 页面]
    end

    GM --> JAPI
    JAPI --> JEB

    EI --> EBUS
    EI --> REG
    EI --> TRG

    JEB -. set_event_bus / publish .-> EBUS

    JEB -- market.tick / market.bar --> EBUS
    JEB -- order.status --> EBUS
    JEB -- position.update --> EBUS
    JEB -- account.update --> EBUS

    EBUS -. 全局共享查询 .-> REG

    STRAT -. 内部 emitEvent .-> STRAT
    STRAT -. 未见稳定订阅全局市场事件 .-> EBUS
    RISK -. 设计上应订阅策略/订单事件 .-> EBUS
    EXEC -. 设计上应订阅风控批准/订单事件 .-> EBUS
    POS -. 设计上应订阅成交/持仓事件 .-> EBUS

    BOOT --> SS
    BOOT --> RS
    BOOT -. 当前未见统一注入 .-> EBUS
    SS --> UI
    RS --> UI
    BT --> UI

    EBUS -. 当前生产代码里未见主业务广泛消费 .-> SS
    EBUS -. 当前生产代码里未见主业务广泛消费 .-> RS
    EBUS -. 当前生产代码里未见主业务广泛消费 .-> UI
```

## 说明

- 已确认接入的部分：引擎层 EventBus 底座、全局注册表、第三方适配层事件发布。
- 已存在但未完全打通的部分：策略、风控、下单执行、持仓/账户与 EventBus 的主业务消费链路。
- 当前应用层更多还是通过 Service 直接驱动 UI，而不是通过统一事件总线更新。

## 对应代码位置

- 引擎创建并注册全局 EventBus：src/engine/src/EngineImpl.cpp
- 全局注册表：src/engine/src/GlobalEventBusRegistry.cpp
- 掘金适配层事件发布：src/thirdParty/src/JujinApi_EventBus.cpp
- 应用启动初始化：src/app/src/AppBootstrap.cpp
- 策略内部事件示例：src/domain/strategies/src/MovingAverageStrategy.cpp

## 现状 vs 目标对比图

```mermaid
flowchart TB
    subgraph Current[当前现状]
        C1[第三方接口]
        C2[JujinApi_EventBus]
        C3[Engine EventBus]
        C4[策略/风控/下单/持仓模块]
        C5[Service 与 QML]

        C1 --> C2
        C2 --> C3
        C3 -. 只有局部发布接入 .-> C4
        C4 -. 多数仍未统一消费 .-> C3
        C5 -. 主要靠直接服务调用 .-> C4
    end

    subgraph Target[目标状态]
        T1[行情/订单/账户外部接口]
        T2[统一 EventBus]
        T3[策略信号模块]
        T4[风控审批模块]
        T5[下单执行模块]
        T6[持仓/账户状态模块]
        T7[StrategyService / RiskService / MonitorService]
        T8[QML 页面]

        T1 --> T2
        T2 --> T3
        T3 -->|strategy.signal| T2
        T2 --> T4
        T4 -->|risk.approval / risk.reject| T2
        T2 --> T5
        T5 -->|order.status / trade.fill| T2
        T2 --> T6
        T6 -->|position.update / account.update| T2
        T2 --> T7
        T7 --> T8
    end
```

### 差异总结

- 当前已经具备事件底座和外部事件发布能力，但业务消费端没有全面接到总线上。
- 目标状态应该是所有关键模块都只通过统一事件主题交互，而不是页面或服务之间大量直接调用。
- 下单执行、风控审批、持仓回流是当前最缺的三段事件链。

## 理想目标交互图

```mermaid
flowchart LR
    subgraph Ingress[接入层]
        MD[行情接入]
        BRK[券商/交易接口]
    end

    subgraph Bus[统一事件总线]
        EBUS[EventBus]
    end

    subgraph Core[核心业务模块]
        MKT[行情处理模块]
        STRAT[策略信号模块]
        RISK[风控审批模块]
        EXEC[下单执行模块]
        POS[持仓与账户模块]
        PERF[绩效与监控模块]
    end

    subgraph Service[服务与展示层]
        SS[StrategyService]
        RS[RiskMonitorService]
        TS[Trade/Order Service]
        UIS[UI 聚合服务]
        QML[QML 页面]
    end

    MD -->|market.tick / market.bar| EBUS
    BRK -->|order.status / trade.fill / account.update| EBUS

    EBUS -->|market.tick / market.bar| MKT
    MKT -->|market.snapshot.updated| EBUS

    EBUS -->|market.tick / market.bar / market.snapshot.updated| STRAT
    STRAT -->|strategy.signal.buy / strategy.signal.sell / strategy.signal.cancel| EBUS

    EBUS -->|strategy.signal.*| RISK
    RISK -->|risk.approval| EBUS
    RISK -->|risk.reject| EBUS
    RISK -->|risk.alert| EBUS

    EBUS -->|risk.approval| EXEC
    EXEC -->|trading.order.submit.request| EBUS
    EBUS -->|trading.order.updated / order.fill| POS
    POS -->|position.update| EBUS
    POS -->|account.update| EBUS

    EBUS -->|trade.fill / position.update / account.update| PERF
    PERF -->|portfolio.pnl.updated / monitor.metric.updated| EBUS

    EBUS -->|strategy.signal.* / trading.order.updated / order.fill| SS
    EBUS -->|risk.alert / risk.approval / risk.reject| RS
    EBUS -->|trading.order.submit.request / trading.order.cancel.request / trading.order.updated| TS
    EBUS -->|market.snapshot.updated / portfolio.pnl.updated / monitor.metric.updated| UIS

    SS --> QML
    RS --> QML
    TS --> QML
    UIS --> QML
```

### 推荐主题划分

- 行情类：market.tick, market.bar, market.snapshot.updated
- 策略类：strategy.signal.buy, strategy.signal.sell, strategy.signal.cancel
- 风控类：risk.approval, risk.reject, risk.alert
- 下单类：trading.order.submit.request, trading.order.cancel.request, trading.order.updated
- 成交类：trade.fill
- 账户持仓类：position.update, account.update
- 展示与监控类：portfolio.pnl.updated, monitor.metric.updated

## 补充规则

1. 不允许使用 QString 做类型判断，QString 仅允许用于调试输出。
2. QVariantMap 仅允许作为 QML 输入参数和返回到 QML 的输出载荷，桥接输出对象名统一为 StrategyManage。