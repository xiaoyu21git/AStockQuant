# 策略、风控、回测职责分层与参数归属

## 1. 目标

本文件用于统一以下三类参数的职责边界，并作为后续代码收口的唯一依据：

1. 策略/因子定义参数
2. 回测会话参数
3. 风控运行参数

要求：

1. 同一语义只能有一个主归属层。
2. 页面只能编辑自己所属层的参数，不能跨层代写、代存、代回填。
3. 结构化合同必须按层存储，不能靠平铺字段或别名回退混用。
4. 回测页可以读取策略定义，但不能反向改写策略定义。
5. 风控页可以读取策略/回测上下文用于展示，但不能持久化策略定义或回测假设。
6. 系统默认值页只能维护运行期规则默认值，不能再承载回测成本默认值并通过运行时规则注入到策略结构。

## 2. 分层定义

### 2.1 策略/因子定义层

职责：描述“这条策略本身怎么选股、怎么调仓、何时止损止盈”。

特征：

1. 脱离某一次回测依然成立。
2. 属于策略模板、策略实例、因子叠加合同本身。
3. 应由策略创建页或策略编辑页维护。

主存储位置：

1. `rule_profile`
2. `factor_overlay`
3. 策略类型专属参数字段

典型参数：

1. `positionSize`
2. `stopLoss`
3. `takeProfit`
4. `maxDrawdownLimit`
5. `rebalanceDays`
6. `factor_overlay.targetPositionCount`
7. `factor_overlay.minimumCompositeScore`
8. 各策略专属参数，例如 `momentumPeriod`、`topN`、`fastPeriod`、`slowPeriod`

### 2.2 回测会话层

职责：描述“本次验证怎么跑”。

特征：

1. 只对某一次回测或研究实验有效。
2. 改变它不会改变策略定义，只会改变本次回测结果。
3. 应由策略回测页或因子回测页维护。

主存储位置：

1. `backtest_runtime`
2. `backtest_universe_*`
3. 回测请求中的 `costSpec`、`executionSpec`、`dataSpec`

典型参数：

1. `startDate` / `endDate` / `backtestPeriod`
2. `initialCapital`
3. `benchmark` / `benchmarkSymbol`
4. `commissionRate`
5. `slippageRate`
6. `dataSourceMode`
7. `marketFilters`
8. `sectorFilters`
9. `excludeSt`
10. `minListingDays`
11. `minTurnoverRate`
12. `maxPositions`
13. `minCompositeScore`
14. 研究增强项，例如 `enableWalkForward`、`enableMonteCarlo`

说明：

1. 当策略启用 `factor_overlay` 时，选股数和最低综合分由策略定义层负责，回测层不得再用 `maxPositions`、`minCompositeScore` 反向覆盖。
2. 因子回测页里的 `forwardDays`、`rebalanceDays`、`benchmarkSymbol`、`commissionRate`、`slippageRate` 属于因子研究运行参数，也归入回测会话层，不属于风控。

### 2.3 风控运行层

职责：描述“运行中允许下什么单、允许承受什么实时风险”。

特征：

1. 面向账户、组合、委托执行与实时监控。
2. 主要由风控页、RiskConfigService、RiskMonitorService 维护和消费。
3. 不应定义策略选股逻辑，也不应定义某次回测假设。

主存储位置：

1. RiskConfigService 持久化配置
2. 风控页动态参数配置
3. RiskMonitorService 运行时判定输入

典型参数：

1. `varWarningPercent`
2. `positionSizingMethod`
3. `maxPositionPercent`
4. `maxTotalExposure`
5. `orderSizeLimit`
6. `turnoverLimit`
7. `slippageLimit`
8. `level1Breaker`
9. `level2Breaker`
10. `level3Breaker`
11. `maxDailyLoss`

### 2.4 全局默认值层

职责：提供默认初值，不拥有业务语义。

规则：

1. 默认值层只能给各业务层提供初始值。
2. 默认值层不能替代业务归属层成为编辑入口。
3. `SystemSettingsPage` 可以维护全局默认初值，但不能改变参数归属。

## 3. 参数归属矩阵

| 参数 | 最终归属层 | 编辑入口 | 结构化归属 | 备注 |
| --- | --- | --- | --- | --- |
| positionSize | 策略/因子定义层 | 策略创建页 | 策略参数 | 不属于风控 |
| stopLoss / stopLossPercent | 策略/因子定义层 | 策略创建页 | rule_profile | 不属于回测会话 |
| takeProfit / takeProfitPercent | 策略/因子定义层 | 策略创建页 | rule_profile | 不属于回测会话 |
| maxDrawdownLimit | 策略/因子定义层 | 策略创建页 | rule_profile | 不属于回测会话 |
| rebalanceDays | 策略/因子定义层 | 策略创建页 | rule_profile | executionPolicy 只能派生，不得反向拥有 |
| targetPositionCount | 策略/因子定义层 | 策略创建页 | factor_overlay | 组合/因子策略专用 |
| minimumCompositeScore | 策略/因子定义层 | 策略创建页 | factor_overlay | 组合/因子策略专用 |
| backtestPeriod | 回测会话层 | 回测页 | backtest_runtime | 不属于风控 |
| initialCapital | 回测会话层 | 回测页 | backtest_runtime / costSpec | 不属于风控 |
| benchmark / benchmarkSymbol | 回测会话层 | 回测页 | backtest_runtime | 不属于风控 |
| commissionRate | 回测会话层 | 回测页 | backtest_runtime / costSpec | 不属于风控页 |
| slippageRate | 回测会话层 | 回测页 | backtest_runtime / costSpec | 不属于风控页 |
| maxPositions | 回测会话层 | 回测页 | backtest_runtime | 仅标准策略回测使用 |
| minCompositeScore | 回测会话层 | 回测页 | backtest_runtime | 当 factor_overlay 生效时禁用 |
| marketFilters / sectorFilters | 回测会话层 | 回测页 | backtest_runtime | 股票池研究过滤 |
| excludeSt / minListingDays / minTurnoverRate | 回测会话层 | 回测页 | backtest_runtime | 股票池研究过滤 |
| positionSizingMethod | 风控运行层 | 风控页 | 风控配置 | 运行期仓位控制，不属于回测页 |
| maxPositionPercent | 风控运行层 | 风控页 | 风控配置 | 单票风险约束，不属于策略创建页 |
| maxTotalExposure | 风控运行层 | 风控页 | 风控配置 | 组合风险预算，不属于策略创建页 |
| varWarningPercent | 风控运行层 | 风控页 | 风控配置 | 实时预警 |
| orderSizeLimit | 风控运行层 | 风控页 | 风控配置 | 委托级限制 |
| turnoverLimit | 风控运行层 | 风控页 | 风控配置 | 日内成交限制 |
| slippageLimit | 风控运行层 | 风控页 | 风控配置 | 实时成交容忍度 |
| level1/2/3Breaker | 风控运行层 | 风控页 | 风控配置 | 账户/组合级熔断 |

## 4. 禁止事项

以下行为一律视为职责越界：

1. 风控页持久化 `backtest_assumptions`、`backtest_runtime` 或任何回测成本参数。
2. 回测页加载风险参数后再用白名单/黑名单清洗兜底。
3. `rebalanceDays` 同时作为 `rule_profile` 输入字段和 `execution_policy` 输入字段。
4. `factor_overlay.targetPositionCount` 与 `maxPositions` 混读。
5. `factor_overlay.minimumCompositeScore` 与 `minCompositeScore` 混读。
6. `slippageLimit` 兼容映射为 `slippageRate`。
7. `positionSizingMethod`、`maxPositionPercent`、`maxTotalExposure` 出现在策略创建页或回测页作为独立编辑入口。
8. `runtimeRuleDefaults.backtestAssumptions` 注入 `backtestAssumptionsSnapshot`。

## 5. 本次收口决定

本次优化先处理确定性最高、不会改变业务含义的混层点：

1. 风控页不再写入任何 `backtestAssumptions`。
2. 风控页不再把 `rebalanceDays` 当作风控参数。
3. 策略回测页从共享元数据中只读取 `backtest` 类参数，不再读取 `risk` 类参数再二次过滤。
4. `rebalanceDays` 只从策略定义层进入，执行频率只允许由其派生。
5. 系统默认值页不再维护和注入回测成本默认值。
6. 风控页不再维护 `maxPositions`；`excludeSt`、`minListingDays`、`minTurnoverRate` 已提为 `StrategyBacktestConfig` 显式字段，不再通过泛化参数容器回捞。
7. 组合运行期目标持仓数只认 `targetPositionCount`；`RiskMonitorService` 和 `TradingConnectionConfigService` 不再把 `maxPositions` 当组合运行期别名。

## 6. 后续继续收口的优先级

1. 继续把页面与服务的“默认值来源”和“业务拥有者”彻底分开。
